# POS-원캡 SPEC 열린 질문 대장

> **이 문서는 정본이 아니다.** POS↔원캡 전산설계서(SPEC)를 읽다가 **명시되지 않았거나 문서 내부가
> 서로 어긋나는 지점**을 모아 발주처에 물어보기 위한 대장이다. 아래 매핑표는 **조사 기록**이며,
> 등급 (다)/(라) 행은 우리 추정일 뿐 구현 근거가 아니다.
>
> **답변이 오면 이 문서를 먼저 고치고** `PRD.md`·코드에 반영한다(`fault_alert_catalog.md`와 같은 규칙).
>
> **왜 별도 문서인가** — 이 목록은 요구사항이 아니라 **발주처와 주고받으며 계속 바뀌는 대장**이다.
> Phase 문서(`PRD.md` / `ROADMAP.md` / `development_plan.md`)에 섞어 두면 그 Phase가 끝난 뒤 아무도
> 갱신하지 않는다.

- **최초 작성**: 2026-09-18 (Phase 29 준비 중)
- **근거 정본**: `spec/국세 베리어프리 키오스크용 전산설계서(POS-원캡)_20260915 (1).pdf`
  (2026-09-15 개정판, **2026-09-18 재배포본** — 파일명의 ` (1)`은 재배포 과정에서 붙은 것이며 같은
  개정일 문서의 최신본이다. 총 23페이지). 같은 폴더 `.hwp`는 DRM 배포용 래퍼라 열리지 않는다. 필드
  확인은 `pos-onecap-spec-expert` 서브에이전트에 위임한다 — 페이지 번호는 모두 이 PDF 기준이다.
- **2026-09-18 재배포본에서 달라진 것**: `902614 #38 카드소유주 주민(사업자)등록번호`의 SET 장소 열에
  **kiosk ○가 추가**됐다(표현 AN13 / POSITION 319는 불변). 그 외 3전문·999999 섹션·응답코드 섹션은
  바이트 단위로 이전과 동일하다.
- **상태**: Q1~Q11 전부 **미질의**. 아직 이 문서를 읽는 코드는 없다.
- **관련 문서**: `PRD.md` §3.3(전문 구조) · `ROADMAP.md` 남은 미확정 사항

---

## 1. 왜 전문 간 필드 연쇄를 아직 구현하지 않는가

국세 결제는 실제 업무 순서상 `501008`(국고 상세 고지내역 조회) → `800000`(카드 정보 조회) →
`902614`(국고 신용카드 승인요청)로 진행되고, 앞 전문의 응답값이 뒤 전문의 요청 필드로 들어간다.
그런데 SPEC을 전수 확인한 결과 **연쇄 근거의 수준이 구간마다 크게 달랐다**(2026-09-18 조사):

- `501008` → `902614`: **명문 규정 있음.** 단 그 안에도 절삭·표현 변환·출처 미지정이 섞여 있다(§2).
- `800000` → `902614`: **명문 규정 전무.** SPEC은 해당 필드를 인터넷지로 API 재조회로 채우라고만
  한다(§3).
- 세 전문의 **호출 순서를 규정한 문장 자체가 문서에 없다**(목차 절 순서가 유일한 암시).

연쇄 매핑을 지금 `PRD.md`와 코드에 고정하면 **우리 추정이 정본으로 굳는다.** 발주처 답변이 오면
문서·코드·화면 세 곳을 되돌려야 한다. 그래서 Phase 29의 결제 화면(`PRD.md` §12)은 **모든 필드를
표현/길이에 맞는 임의값으로 채워 보내는 방식**으로 먼저 만들고(연쇄 없음), 연쇄는 이 문서의 질의가
해소된 뒤 구현한다(2026-09-18 사용자 확정). 이 화면은 최종적으로 **실사용 결제 화면**이며 임의값은
1단계 한정이다 — 그 덕분에 Phase 29의 검증 대상이 연쇄 규칙이 아니라 **프레이밍(`#0` 길이)·POSITION
정합성·CP949 왕복·원캡이 채우는 카드리딩 필드**로 좁혀진다.

### 근거 등급 표기

| 등급 | 뜻 |
|---|---|
| (가) | SPEC 원문에 "SET하라"는 **명시적 지시**가 있음 |
| (나) | 지시문은 없으나 **이름이 완전히 동일**해 사실상 자명 |
| (다) | **의미만 겹침** — 우리 추정 |
| (라) | 매칭 후보 없음 |

---

## 2. 조사 기록 — `501008` 응답 → `902614` 요청

### 2.1 공통부 (등급 (가))

p.7 `□ (#11)이용기관 분류코드, (#12)이용기관 지로번호` 표의 `902614` 행에 원문 그대로:

> `※'501008'(상세 고지내역 조회)의 응답부에 전달한 값 SET`

| `902614` 요청 | `501008` 응답 | 변환 | 등급 |
|---|---|---|---|
| `#11` 이용기관 분류코드 | `#11` 같은 필드의 응답값 | 그대로 | (가) p.7 |
| `#12` 이용기관 지로번호 | `#12` 같은 필드의 응답값 | 그대로 | (가) p.7 |

이 표에 **`800000`은 포함되지 않는다** → Q9.

### 2.2 업무부 (p.16 헤더 `□ 고지내역정보와 동일하게 SET` + 19개 필드 목록)

"고지내역정보"는 `501008` 응답을 가리킨다. 목록 자체는 (가)지만, **어느 응답 필드에서 오는지는
필드명 대조로 찾아야 했다** — 이름이 다르거나 길이·표현이 어긋나는 행이 섞여 있다.

| `902614` 요청 | 표현·길이 | `501008` 응답 | 표현·길이 | 필요 변환 | 등급 |
|---|---|---|---|---|---|
| `#14` 주민(사업자,법인)등록번호 | AN13 | `#17` 납세의무자 번호 | AN13 | 그대로 | (가) p.16 명문 "납세의무자 번호 SET" |
| `#15` 전자납부번호 | AN19 | `#14` 전자납부번호 | AN19 | 그대로 | (가) 이름 일치 |
| `#16` 납부 순번 | **AN**3 | `#15` 납부 순번 | **N**3 | 표현 N→AN (자릿수 동일, 값 그대로) | (가) 이름 일치 |
| `#17` 예비 정보 FIELD | AN8 | (동일 목록 포함) | — | 그대로 | (가) 목록 포함 |
| `#18` 징수과목코드(세목코드) | **N**7 | `#20` 징수과목코드(세목코드) | **AN**7 | 표현 AN→N | (가) 이름 일치 |
| `#19` 징수관 계좌번호 | AN6 | `#22` 징수관 계좌번호 | AN6 | 그대로 | (가) 이름 일치 |
| `#20` 징수기관명 | AHN**20** | `#19` 징수기관명 | AHN**40** | **절삭 40→20** | (가) 이름 일치 / 절삭 규칙은 미기재 |
| `#21` 징수과목명 | AHN**20** | `#21` 징수과목명 | AHN**40** | **절삭 40→20** | (가) 이름 일치 / 절삭 규칙은 미기재 |
| `#22` 소계정 | N1 | `#23` 소계정 | N1 | 그대로 | (가) 이름 일치 |
| `#23` 징수결의회계년도 | N4 | `#24` 징수결의회계년도 | N4 | 그대로 | (가) 이름 일치 |
| `#24` 납부세액-본세 | N15 | `#30` 본세 | N15 | 그대로 | (가) |
| `#25` 납부세액-교육세 | N15 | `#32` 교육세 | N15 | 그대로 | (가) |
| `#26` 농어촌특별세 | N15 | `#31` 농어촌특별세 | N15 | 그대로 | (가) |
| `#27` 납부 세액(합계) | N15 | **출처 미지정** | — | 추정: `#24+#25+#26` | **(다) → Q5** |
| `#30` 납기내후구분 | AN1 | `#44` 납기내후구분 | AN1 | 그대로 | (가) 이름 일치 |
| `#31` 납기일자 | N8 | `#26`(납기내) 또는 `#28`(납기후) | N8 | 추정: `#30` 값으로 선택 | **(다) → Q6** |
| `#36` 납부자 주민(사업자)등록번호 | AN13 | `#17` 납세의무자 번호 (?) | AN13 | 추정: `#14`와 동일 출처 | **(다) → Q7** |
| `#37` 납부자 성명 | **AHNS**10 | `#18` 납세의무자명 | **AHN**40 | **절삭 40→10 + 표현 AHN→AHNS** | (가) 목록 포함 / 절삭 규칙 미기재 |
| `#38` 카드소유주 주민(사업자)등록번호 | AN13 | **출처 미지정** | — | 미정 | **(다) → Q8** |

`#38`은 2026-09-18 재배포본에서 표에 kiosk ○가 추가되어 **"채워야 하는가"는 해소**됐다. 다만 설명절
(p.16)은 `□ 고지내역정보와 동일하게 SET` 목록에 `(#38)`을 넣어 두기만 했고 `#14`처럼 출처 필드를
지목하지 않았다 — 그리고 **`501008` 응답에는 "카드소유주"라는 필드가 아예 없다.** 값이 카드리딩
결과인지, 별도 입력인지, `#14`와 같은 값의 복제인지가 남는다 → Q8.

### 2.3 `501008`과 무관하게 kiosk가 자체 생성하는 필드 (참고)

`#32` 납부일자(납부 당일 `YYYYMMDD`), `#35` 연락전화번호, `#39` 납부 이용 시스템(고정 `"O"`),
`#40` 기 납부 이용 시스템(인터넷지로 응답으로 전달), `#41` 납부 형태 구분(고정 `"Q"`),
`#42` 키오스크 고유번호(자체 생성 규칙). — p.16~17.

---

## 3. 조사 기록 — `800000` 응답 → `902614` 요청

**등급 (가)는 하나도 없다.** `902614` 설명절(p.16~17)·흐름도(p.14)·개요(1장, p.4) 어디에도
"800000" 또는 "카드 정보 조회"라는 표현이 **한 번도 등장하지 않는다.** SPEC은 해당 필드를
**인터넷지로 API 3종 재조회**로 채우라고만 지시한다:

- `#28` 수수료 — p.16 `API「카드 수수료 금액 조회(/igiro/v1/services/card-fee)」이용, 인터넷지로
  수납센터에서 재검증함`
- `#33` 카드사코드 — p.17 `API「납부가능 신용카드(간편결제)사 조회(/igiro/v1/services/avail-card)」통해
  서비스 가능한 카드사 선 조회 후 "card_code"(2자리) SET`
- `#34` 할부개월수 — p.17 `API「신용카드 할부 가능 개월 조회(/igiro/v1/services/card-installment)」통해
  해당 카드사의 가능한 할부 개월 수 선 확인 후 SET`

이름·의미가 겹치는 후보는 `902614` kiosk 구간 29개 필드 중 **4개뿐**이다:

| `902614` 요청 | 표현·길이 | `800000` 응답 | 표현·길이 | 문제 | 등급 |
|---|---|---|---|---|---|
| `#33` 카드사 코드 | **N**2 | `#17` 카드사 코드 | **AN**2 | 표현 AN→N | **(나) → Q1** |
| `#34` 할부 개월 수 | N2 | `#22` 카드 할부개월 LIST | AN60 (최대 30개) | 리스트→단일값, **선택 규칙 SPEC 없음** | **(다) → Q2** |
| `#28` 수수료 | N**15** | `#24` 납부대행 수수료 금액 | N**12** | 길이 12→15 | **(다) → Q3** |
| `#29` 총 납부 금액 | N**15** | `#25` 합계금액 | N**12** | 길이 12→15, 명칭 상이 | **(다) → Q3** |

`800000` 응답의 나머지 필드(`#18` 카드사명, `#19` 체크카드 여부, `#20` 납부가능 시간 여부,
`#21` 포인트 납부 가능 여부, `#23` 포인트 할부개월 LIST, `#26` 납부대행 수수료율,
`#27` API 세부 응답코드)는 `902614`에 대응 필드가 **없다**(라) — `800000` 자체에서 소비되는
(화면 표시/오류 진단) 값으로 보인다.

---

## 4. 발주처 질의 목록

| ID | 질문 | 근거 페이지 | 상태 | 답변 요지 | 반영 위치 |
|---|---|---|---|---|---|
| Q1 | `902614` `#33` 카드사코드는 `800000` 응답 `#17` 카드사코드(이름 완전 일치)를 그대로 재사용하는 것인가, 아니면 항상 별도 API(`avail-card`)를 재호출해 새로 받아야 하는가 | p.13 `#17`, p.17 `#33` | 미질의 | | |
| Q2 | `902614` `#34` 할부개월수는 `800000` 응답 `#22`/`#23` 할부개월 LIST 중 사용자가 선택한 값인가, 매번 `card-installment`를 재호출하는가. 재호출한다면 `800000`의 LIST는 화면 표시용으로만 쓰이는가 | p.13 `#22`/`#23`, p.17 `#34` | 미질의 | | |
| Q3 | `902614` `#28` 수수료·`#29` 총납부금액은 `800000` 응답 `#24`/`#25`와 값이 같아야 하는가. 같다면 길이 N12→N15 변환 규칙은 무엇인가. 다르다면 매 거래마다 `card-fee`로 독립 계산되는가 | p.13 `#24`/`#25`, p.16 `#28`/`#29` | 미질의 | | |
| Q4 | p.16 `"(#29)총 납부 금액: (#27)납부 세액 + (#29)수수료를 SET"` 문장의 `(#29)수수료`는 `(#28)수수료`의 오탈자인가 (자기 참조로 되어 있음) | p.16 | 미질의 | | |
| Q5 | `902614` `#27` 납부세액(합계, N15)의 정확한 산출식 — `#24` 본세 + `#25` 교육세 + `#26` 농특세 단순합인가, 다른 세목이 더 있는가 | p.15 `#24`~`#27`, p.16 | 미질의 | | |
| Q6 | `902614` `#31` 납기일자는 `501008` 응답 `#26`(납기일-납기내) / `#28`(납기일-납기후) 중 어느 것인가. `#30` 납기내후구분 값에 따라 선택하는 것이 맞는가 | p.9 `#26`/`#28`, p.15 `#30`/`#31` | 미질의 | | |
| Q7 | `902614` `#36` 납부자 주민(사업자)등록번호와 `#14` 주민(사업자,법인)등록번호는 항상 같은 값(`501008` 응답 `#17`)인가, 서로 다른 소스를 가질 수 있는가(예: 대리납부) | p.15 `#14`/`#36`, p.16 | 미질의 | | |
| Q8 | `902614` `#38` 카드소유주 주민(사업자)등록번호의 **값 출처**는 무엇인가 — 설명절(p.16)은 "고지내역정보와 동일하게 SET" 목록에 넣어 두었을 뿐 출처 필드를 지목하지 않았고, `501008` 응답에는 "카드소유주" 필드가 없다. `#14`(납세의무자 등록번호)와 같은 값을 복제하는가, 카드리딩 결과에서 추출하는가, 화면에서 별도로 입력받는가(대리납부·타인 카드 결제 시 납세의무자 ≠ 카드소유주일 수 있음) | p.15, p.16 | 미질의 | **SET 주체는 해소** — 2026-09-18 재배포본에서 표(p.15)에 kiosk ○ 추가. 출처만 남음 | |
| Q9 | `800000` 요청 `#11`(이용기관 발행기관 분류코드)/`#12`(이용기관 지로번호)/`#15`(납부세액)는 SET 장소가 kiosk로만 표시돼 있는데, `501008` 응답값을 그대로 넣어야 하는가. `902614`의 `#11`/`#12`에는 "501008 응답부에 전달한 값 SET" 명문이 있으나(p.7) 그 표에 `800000`은 빠져 있고, `800000`에는 "전문 구성 요소 설명" 절 자체가 없다 | p.7, p.13 `#11`/`#12`/`#15` | 미질의 | | |
| Q10 | `800000`과 `902614` 사이의 **호출 순서·데이터 재사용 관계를 규정하는 문장이 SPEC에 전혀 없다** — 두 전문이 완전히 독립적으로(매번 API 재조회) 처리되는 설계가 맞는가, 아니면 kiosk가 `800000` 응답을 세션 내에 캐시해 `902614` 작성 시 재사용해도 되는가 | 문서 전체(특히 p.5 전문 종류 표에 `800000` 누락, p.14 `902614` 흐름도) | 미질의 | | |
| Q11 | **문서 관리 결함 4건 정정 요청** — ① 목차(p.3)와 변경이력표(p.2)가 신규 섹션(마. 직전 거래 조회 전문 999999, 3장 "2) 원캡 응답 코드")을 전혀 반영하지 않았다(변경이력은 여전히 "2026.08. ver 0.1 최초 작성" 한 줄뿐). ② 공통부분 "전문 종류" 표(p.5)에 `800000`이 아예 빠져 있다. ③ `902614` 총 길이 행이 표에 미기재다(`#54` POSITION 1328 + AN172 = 1500으로 역산해야 함). ④ p.16의 `"(#29) = (#27) + (#29)"` 자기참조(Q4와 동일 건) | p.2, p.3, p.5, p.14~18 | 미질의 | **2026-09-18 재배포본에서도 4건 전부 미수정 확인** | |

---

## 5. 이미 해소된 항목 (기록 보존)

이 문서를 만들기 전에 확정된 SPEC 모순·불명 사항은 `PRD.md`에 이미 반영돼 있다 — 중복 관리하지
않고 포인터만 남긴다.

| 항목 | 결론 | 반영 위치 |
|---|---|---|
| `#51` 암호화된 비밀번호 정보의 SET 주체 (표는 kiosk, 설명절은 보안리더기) | **원캡이 PIN 화면에서 입력받아 채운다**(2026-08-26 확정) | `PRD.md` §4.12, §10 |
| `#46` 암호화된 카드정보의 내부 구성 (SPEC에 길이 헤더 서술 없음) | `"0"` + 3자리 길이 + 페이로드(2026-09-01 사용자가 실기 캡처로 확정) | `PRD.md` §10 |
| 인코딩·패딩 규칙 (SPEC 원문 미기재) | CP949 / N=우측정렬 0패딩, 그 외=좌측정렬 space패딩 (국내 관례, **발주처 확인 대기**) | `PRD.md` §10 |
| 원캡 자체 응답코드 체계 (`E`/`R`/`D`) | 원캡이 정의해 발주처에 제공 → **2026-09-15 개정판 3장 "2) 원캡 응답 코드"로 SPEC에 등재됨** | `PRD.md` §4.11 |
| 직전 거래 조회 전문(999999) | 원캡이 제안 → **2026-09-15 개정판 "마." 절로 SPEC에 등재됨**. `#4` 거래구분 코드는 여전히 발주처 채번 대기 | `PRD.md` §3.4, `ROADMAP.md` 미확정 #5 |
