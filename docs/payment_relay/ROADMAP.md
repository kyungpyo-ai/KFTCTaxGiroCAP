# ROADMAP: KFTCTaxGiroCAP 결제 중계 기능 (2차 개발)

> 이 문서는 `PRD.md`(무엇을 만들지)를 기준으로, **어떤 순서로 얼마나 작은 단위로 구현할지**를 정의한다.
> Task 단위 작업 지시와 완료 조건 체크리스트는 `development_plan.md`(실행계획서)에서 다룬다.
>
> **Phase 번호는 1차 범위(`docs/home_reader_setup/ROADMAP.md`, Phase 0~6)에서 이어진다** — 같은 앱
> (`KFTCOneCAP.Wpf`)을 계속 확장하는 것이므로 번호를 새로 시작하지 않고 Phase 7부터 붙인다. 1차 범위의
> 진행 이력·결정 사항이 필요하면 그 문서를 참고한다.

## 작업 방식 (바이브코딩 규칙 — 1차 범위와 동일)

1. Phase는 순서대로 진행한다. 이전 Phase의 "완료 기준"을 통과하지 못하면 다음으로 넘어가지 않는다.
2. 각 Phase 종료 시: `dotnet build` 성공 + 실제 실행 검증. 하드웨어/외부 DLL이 필요한 항목은 **검증한 것만**
   완료로 표시하고, 검증하지 못한 범위는 그 사실을 명시한다(추측으로 완료 처리하지 않는다).
3. PRD와 실제 구현이 어긋나는 지점을 발견하면, 코드보다 먼저 `PRD.md`를 갱신한 뒤 구현을 맞춘다.
4. 각 Phase는 되도록 하나의 커밋 단위로 마무리한다(사용자가 커밋을 요청할 때).
5. 막히거나 PRD에 없는 결정이 필요하면 즉시 사용자에게 확인 — 임의로 스펙을 정하지 않는다.

## 전제

대상은 1차 범위와 동일하게 `KFTCOneCAP.Wpf` 하나다(별도 실행 파일로 분리하지 않음, 2026-08-18 확정).
Reader DLL 연동 참조 자료(`docs/reader_dll/`, `vendor/ReaderSerial/`)와 전담 서브에이전트
(`reader-pinpad-spec-expert`, `reader-dll-integration-developer`)는 이미 준비돼 있다 — Phase 9부터 활용한다(Phase 7/8은 구조·환경 정비 단계라 DLL을 호출하지 않는다).

> **핵심 개발 원칙 (KFTCReaderDLL 프로젝트에서 검증된 전략을 그대로 재사용, PRD §10 근거)**: 실제 통신
> 전문(POS↔KFTCTaxGiroCAP, KFTCTaxGiroCAP↔VAN)이 아직 미확정이므로, 매 Phase마다 **임시 테스트 전문으로
> 로직(Queue/동시성/상태 전이)을 먼저 검증**하고, 전문 생성/파싱은 별도 계층으로 분리해 나중에 실제 SPEC이
> 확정되면 그 계층만 교체하면 되도록 구현한다. 전문 파싱 로직을 Reader 제어/결제 Flow 로직과 뒤섞지 않는다.

## 진행 상태

| Phase | 내용 | 상태 |
|---|---|---|
| 7 | **MVVM 전환** — 1차 화면(홈/리더기 설정) ViewModel 분리, 이후 전 Phase의 UI 작성 방식 통일 | ✅ 완료 |
| 8 | 기반 정비 — `PlatformTarget=x86` 전환, 두 DLL 배치 및 로드 스모크 | ✅ 완료 |
| 9 | Reader DLL P/Invoke 바인딩 + 파일럿 명령(`0x60` 초기화) 왕복 | ✅ 완료(조건부 — 화면 E2E는 Phase 12로 이월, 아래 참고) |
| 10 | Reader 서비스 계층 — 명령 4종 확장, Protocol 계층 분리, 단일 유효 응답 게이트 | ✅ 완료(실장비 2대 경합/무효화·실제 카드 태그 경쟁·`PORT_NOT_OPEN` 실장비 재현+재연결 복구까지 전부 검증. `SEND_FAIL`만 API 레벨로 결정적 유도 불가해 미검증 — 구조적 이유 실측 확인, 아래 참고) |
| 11 | 로컬 DB(SQLite) — 무결성 체크 이력 저장/조회 (PRD §7) | ✅ 완료 |
| 12 | 리더기 설정 화면 실동작 배선 (PRD §6 + 1차 범위 보류 4항목) | ✅ 완료(실장비 COM5/COM3 2대로 초기화·상태체크·무결성체크·DB 저장/조회까지 E2E 검증. 응답코드 `08`/1단계(0x71) 실패 시 null 저장/결과 칩 실패 색상은 물리적 카드 태그·케이블 분리가 필요해 코드 검토로 대체 — 아래 참고) |
| 13 | 결제 알림창 UI — IC/FALLBACK/PROCESSING, Topmost, ESC Hook (PRD §5) | ✅ 완료(Opus 검증 리뷰 5건 수정까지 반영. 시각 정교화는 별도 재개 예정) |
| 14 | 소켓 서버 + 단일 워커 Queue (PRD §3) | ✅ 완료(2026-08-24) |
| 15 | 결제 Flow 조립 — 무결성 선행 → 카드 리딩 → FALLBACK/`12`/실패 분기 (PRD §4.1~§4.7) | ✅ 완료(체크포인트1/2 Opus 검증 리뷰 및 수정 반영, 2026-08-25) |
| 16 | 사용자 취소 & Timeout(120초) 동시성 확정 (PRD §4.8~§4.9, §8) | ✅ 완료(2026-08-26, 실장비 2대 검증 포함) |
| 17 | **POS↔원캡 실제 전문 적용** — 임시 전문을 SPEC 3종(501008/800000/902614)으로 교체 | ✅ 완료(2026-08-27, 최종 검증 + 실장비 검증 포함) |
| 18 | **카드 비밀번호 입력** — 알림창 4번째 상태 + Flow 통합, `#51` 채움, 물리 키보드 입력(P18-8) | ✅ 완료(2026-08-28, 최종 검증 + 실장비 검증 5개 시나리오 포함. 실장비 검증 중 H-2(VAN 스텁 PIN 유출) 발견·수정, 사용자 요청으로 키보드 입력 지원(P18-8) 추가) |
| 19 | **키오스크 시뮬레이터** — POS 역할 테스트 프로그램(WinForms, 업체 제공용) | ⬜ 대기 |
| 20 | VAN 연동 — `KFTC_GIRO.dll`/`FNAISCRDVAN` (PRD §2.3, §4.10) | ⬜ 대기 |
| 21 | 통합 검증 & 안정성 (PRD §9) | ⬜ 대기 |

> **Phase 재배치(2026-08-26)**: POS↔원캡 구간 전산설계서가 확보되어(`spec/국세 베리어프리 키오스크용
> 전산설계서(POS-원캡)_20260826.pdf`) 실제 전문이 확정됐다. 이를 **VAN 연동보다 먼저** 적용하기로 해
> Phase 17~19를 새로 삽입하고 기존 17(VAN)→20, 18(통합 검증)→21로 밀었다. 근거: ① 지금까지의
> "POS 요청 1건 = 결제 1건" 전제가 SPEC의 3전문 구조와 맞지 않아 `PaymentOrchestrator` 재구성이 선행돼야
> 하고, ② **POS↔원캡 구간과 원캡↔VAN 구간의 전문 형식이 동일**하므로 이 Phase를 먼저 끝내면 Phase 20이
> 크게 가벼워진다(`Protocol/Van/`을 따로 설계하지 않고 같은 전문을 채워 넘기는 형태가 된다).
>
> Phase 18(비밀번호 입력)은 **SPEC 확보 과정에서 새로 드러난 요구사항**이다 — `#51 암호화된 비밀번호 정보`가
> 설계서에는 kiosk 담당으로 표시돼 있으나 **설계서 오류이고 원캡이 입력받는 것이 맞다**(2026-08-26 사용자
> 확정). 기존 로드맵 어디에도 없던 화면이라 Phase를 따로 뒀다 — Phase 13(알림창 UI를 독립적으로 완성하고
> Flow 연결은 Phase 15에서)과 같은 패턴이며, Phase 17은 `#51`을 space 스텁으로 두면 전문 계층만으로 완결
> 검증이 된다. 시뮬레이터(19)를 그 뒤에 둔 것은 **완성된 3단 흐름**을 몰아볼 수 있어야 키오스크 업체에
> 넘길 샘플로서 의미가 있기 때문이다.

(상태 값: ⬜ 대기 / 🔄 진행중 / ✅ 완료 / ⏸ 보류)

각 Phase의 Task 단위 작업 지시/완료 조건 체크리스트는 **별도 실행계획서**(`development_plan.md`,
Phase 착수 직전에 작성)에서 다룬다 — 이 ROADMAP은 "무엇을 어떤 순서로, 어디까지 하면 끝인지"만 정의한다.
(1차 범위 Phase 0~6은 실행계획서 없이 이 문서 하나로 진행했으나, 2차 범위는 하드웨어/외부 DLL/동시성이
얽혀 Task 단위가 훨씬 잘게 쪼개지므로 KFTCReaderDLL 프로젝트에서 검증된 3단 구성(PRD → ROADMAP →
실행계획서)을 따른다.)

---

## 계층 구조 (Phase 8~21 공통 설계 원칙)

PRD §10이 "실제 통신 전문이 확정되면 전문 생성/파싱 부분만 교체하고 Reader 제어·결제 Flow는 손대지 않는다"를
요구하므로, **전문(電文)을 아는 코드와 흐름을 아는 코드를 물리적으로 분리**한다. 새로 만들 폴더는 다음과 같다.

```
src/KFTCOneCAP.Wpf/
├─ Interop/          네이티브 경계 — P/Invoke 선언만. 업무 로직 금지
│   ├─ ReaderSerialNative.cs   (vendor/ReaderSerial/CSharpSample 기반 포팅)
│   └─ KftcGiroNative.cs       (FNAISCRDVAN, ANSI char* 마샬링)
├─ Protocol/         전문 생성/파싱 — 실제 SPEC 확정 시 "여기만" 교체되는 계층
│   ├─ Reader/         0x2B 요청 빌더, 0x3B/0x71/0x72 응답 파서
│   ├─ Pos/            POS 소켓 요청/응답 전문 (현재 임시 테스트 전문)
│   └─ Van/            VAN 요청/응답 전문 (현재 임시 테스트 전문)
├─ Services/         흐름/상태 — 전문 바이트 배열을 직접 해석하지 않는다
│   ├─ Reader/          ReaderService (포트 생명주기, 명령 송신, 콜백 정규화)
│   ├─ Van/             VanService
│   ├─ Pos/             PosSocketServer
│   ├─ Storage/         IntegrityCheckStore (SQLite)
│   └─ Payment/         TransactionQueue, PaymentOrchestrator
└─ ViewModels/       화면 상태/명령 — Phase 7에서 신설. View(XAML)와 Services 사이의 유일한 접점
```

**의존 방향 규칙**: `ViewModels → Services → Protocol → Interop` 단방향. `Protocol`은 `Services`를 알지 못하고,
`Services`는 바이트 오프셋·필드 길이를 직접 다루지 않는다(반드시 `Protocol`이 만든 결과 객체를 받는다).
`Services`는 `ViewModels`·WPF 타입(`Visibility`, `Dispatcher` 등)을 알지 못한다 — 스레드 마샬링은 ViewModel
쪽 책임이다. 이 규칙이 지켜지면 실제 SPEC 확정 시 변경이 `Protocol/` 안에서 멈춘다 — 매 Phase 완료 기준에
이 점검을 포함한다.

---

## Phase 7 — MVVM 전환 (1차 화면 리팩터링)

**목표**: 1차 범위에서 코드비하인드로 구현된 두 화면을 ViewModel 기반으로 옮겨, **Phase 8 이후의 모든 UI
작업이 하나의 방식만 따르도록** 만든다. 화면의 겉모습과 동작은 **하나도 바뀌지 않는다**(순수 내부 구조 변경).

> **왜 지금인가**: 1차 범위(Phase 0~6)는 `CommunityToolkit.Mvvm` 패키지를 넣어두고도 실제로는 전부
> 코드비하인드로 구현됐다(`HomeWindow.xaml.cs` 245줄, `ReaderSetupWindow.xaml.cs` 416줄). 2차 범위는 여기에
> 리더기 2대 이중화·소켓 서버·비동기 결제 Flow·타임아웃 경합이 얹히므로, 지금 구조를 잡지 않으면 코드비하인드가
> 감당할 수 없는 크기로 자란다. 또한 나중에 전환하면 **이미 검증된 2차 코드까지 다시 건드려야** 하므로
> 전환 비용이 계속 커진다 — 가장 싼 시점이 지금이다(2026-08-19 사용자 확정).
>
> **두 화면의 성격이 다르다**는 점을 전제로 한다. `ReaderSetupWindow`는 레지스트리 로드/저장, dirty-check,
> 조회 결과, busy 상태 등 **ViewModel로 옮길 로직이 대부분**이다. 반면 `HomeWindow`는 트레이 아이콘(WinForms
> interop), DWM 타이틀바, 창 워밍업 등 **본질적으로 View/OS 책임**이라 옮길 것이 적다 — 무리하게 전부
> ViewModel로 밀어 넣지 않는다(형식만 MVVM이고 실속이 없는 구조가 된다).

- [x] `ViewModels/` 폴더 신설 및 ViewModel ↔ View 연결 방식 확정(`DataContext` 설정 위치를 한 가지로 통일)
- [x] `ReaderSetupViewModel` — 레지스트리 로드/저장, dirty-check, COM 콤보 선택 상태, "미사용" 연동에 따른
      활성/비활성, busy 상태(동시 1작업 제한), 조회 결과 컬렉션을 ViewModel로 이관
- [x] 코드비하인드의 **`x:Name` 직접 조작을 바인딩으로 교체** — `IsEnabled`/`Visibility`/`Content`를 코드에서
      대입하지 않는다. 남는 것은 View 고유 관심사(Popup 배치, ScrollViewer 정렬 보정 등)뿐이어야 한다
- [x] `HomeWindow` — 카드 클릭 등 "무엇을 할지"만 ViewModel(Command)로 옮기고, 트레이/DWM/워밍업은
      **코드비하인드에 그대로 남긴다**(그 판단 근거를 주석으로 남긴다)
- [x] 레지스트리 접근을 `Services/Settings/`(가칭)로 분리 — Phase 9 이후 결제 Flow도 같은 값(COM 포트)을
      읽어야 하므로(PRD §2.2.1), ViewModel 안에 레지스트리 코드를 두면 재사용이 불가능하다
- [x] 액션 버튼 5종/조회의 3초·2초 **스텁 동작은 그대로 유지**한다 — 실통신 교체는 Phase 12 몫이며, 이번
      Phase에서 같이 건드리면 회귀 원인을 구분할 수 없다

**완료 기준**: 두 화면의 **모든 기존 동작이 1차 범위와 동일**함을 실행으로 확인한다(콤보 "미사용" 연동,
버튼 로딩 스텁, 조회 더미 행 수, 레지스트리 저장/로드, 취소 dirty-check 확인창, 트레이 최소화/복원).
`ReaderSetupWindow.xaml.cs`에 업무 로직이 남아 있지 않고, 코드비하인드에서 `x:Name` 요소의 상태를 직접
대입하는 코드가 없다.

> **완료 결과**: Task 단위 상세 완료 조건·검증 내역은 `development_plan.md` P7-1~P7-5에 기록돼 있다(트레이
> 아이콘 더블클릭 복원·우클릭 메뉴는 접근성 트리 한계로 자동화 검증 불가 — 해당 코드는 이 Phase에서
> 무수정이라 회귀 위험 낮음, P7-4/P7-5 참고). 이 섹션의 체크박스는 2026-08-20 코드 재검토에서 실제
> 코드(`grep`으로 `.Visibility =`/`.IsEnabled =`/`.Content =`/`.ItemsSource =` 직접 대입 없음, `Views/`에
> 레지스트리 경로 없음, `ViewModels/`에 WinForms/`Window` 타입 참조 없음, `ReaderSetupWindow.xaml.cs`가
> 416줄→165줄로 축소되고 남은 코드가 전부 View/OS 책임임을 확인)를 대조해 뒤늦게 갱신한 것이다 — 구현은
> 이보다 먼저 완료돼 있었고 이 문서만 갱신이 누락돼 있었다.

---

## Phase 8 — 기반 정비 (x86 전환 + DLL 배치/로드 스모크)

**목표**: 두 네이티브 DLL을 로드할 수 있는 빌드 환경을 갖추고, 기존 화면에 회귀가 없음을 확인한다.

`ReaderSerial.dll`(Win32/x86 전용)과 `KFTC_GIRO.dll`(32bit) 모두 x86이므로, 현재 `PlatformTarget` 미지정
(=AnyCPU, 64비트 OS에서 64비트 프로세스로 기동)인 상태로는 **실행 시점에 `BadImageFormatException`이 난다.**
이 전환은 뒤로 미룰수록 비싸지므로 가장 먼저 한다. 또한 `KFTC_GIRO.dll`은 `MFC42.DLL`/`MSVCRT.dll`/
`WSOCK32.dll`에 의존(PRD §2.3)하는데 이 의존성 충족 여부는 **로드를 시도해봐야만** 알 수 있으므로, 이번
Phase에서 로드 스모크까지 끝내 리스크를 일찍 드러낸다(실제 함수 호출은 Phase 20 — 재배치 전 번호로는 17).

- [x] `KFTCOneCAP.Wpf.csproj`에 `<PlatformTarget>x86</PlatformTarget>` 명시, `dotnet build`/실행 확인
- [x] 빌드 산출물 옆에 두 DLL이 복사되도록 csproj 배선 — `KFTC_GIRO.dll`을 `docs/payment_relay/dll/`(=문서
      폴더)에서 **`vendor/KftcGiro/`로 옮겨 `vendor/ReaderSerial/`과 위치 규칙을 통일**했다(`docs/`는 문서,
      `vendor/`는 외부 바이너리). csproj의 `None Include` + `CopyToOutputDirectory` + `<Link>`로 출력 폴더
      루트에 두 DLL을 복사한다
- [x] 두 DLL의 로드 스모크: 앱 기동 시 `LoadLibrary` 수준 확인 → 성공/실패와 실패 사유를 로그로 남긴다.
      **로드 실패해도 앱이 죽지 않고 오류로 처리**되어야 한다(PRD §9)
- [x] 진단/오류 로그 기록 수단 확보 — 이후 모든 Phase가 하드웨어·외부 DLL 오류를 남겨야 하므로 최소한의
      파일 로깅을 여기서 정한다(과도한 로깅 프레임워크 도입은 하지 않는다)
- [x] 회귀 확인: 홈 화면·리더기 설정 화면이 x86 전환 후에도 1차 범위와 동일하게 렌더링/동작

**완료 기준**: x86으로 빌드·실행되고 홈/리더기 설정 화면에 시각적·기능적 회귀가 없다. 두 DLL의 로드 결과가
로그로 확인되며, 로드 실패 시에도 앱이 정상 기동한다.

> **완료 결과(2026-08-19)**: 이 개발 PC에서는 `ReaderSerial.dll`·`KFTC_GIRO.dll` 둘 다 로드 성공(로그에
> "DLL 로드 스모크 성공" 기록). 다만 이는 이 PC에 `MFC42.DLL`/`MSVCRT.dll`/`WSOCK32.dll`이 이미 갖춰져
> 있음을 확인한 것일 뿐이며, **실제 배포 대상 PC에서도 동일하다는 보장은 아니다** — 배포 시점에 재확인
> 필요(자세한 검증 내역은 `development_plan.md` P8-4 참고).

---

## Phase 9 — Reader DLL P/Invoke 바인딩 + 파일럿(`0x60` 초기화)

**목표**: 명령 1종만으로 P/Invoke 계약·콜백·스레딩이 옳다는 것을 먼저 증명한다.

> **파일럿 우선 전략** (KFTCReaderDLL 프로젝트에서 검증된 방식): 전문 4종을 한 번에 붙이지 않는다. 초기화
> (`0x60`→`0x70`, Data 없음)만으로 왕복이 성립하는 것을 확인한 뒤 Phase 10에서 나머지로 확장한다. P/Invoke는
> 시그니처가 한 글자만 어긋나도 스택이 깨져 진단이 어려우므로, 변인을 최소화한 상태에서 먼저 맞춘다.

- [x] `Interop/ReaderSerialNative.cs` — `vendor/ReaderSerial/CSharpSample/ReaderSerialNative.cs`를 포팅한다.
      **새로 설계하지 말고 검증된 선언을 그대로 가져온다**(`[UnmanagedFunctionPointer(StdCall)]` 델리게이트,
      `IntPtr data` + `Marshal.Copy`). `vendor/ReaderSerial/ReaderSerial.h`와 시그니처를 1:1 대조
- [x] 콜백 델리게이트 인스턴스를 필드로 붙잡아 GC 수거를 막는다(`CallbackOnCollectedDelegate` 방지)
- [x] 콜백 → UI 스레드 전달 규칙 확립: 콜백 안에서 `data`를 **즉시 복사**한 뒤 `Dispatcher`로 넘긴다.
      콜백 안에서 UI를 직접 건드리지 않는다(콜백은 리더기 수신 스레드에서 동기 호출됨) — Phase 9 파일럿
      범위에서는 `Services/Reader/ReaderService`가 복사까지만 책임지고(계층 규칙상 Dispatcher를 모름),
      `TaskCompletionSource` 기반 `await`로 결과를 돌려주는 방식이라 이번 배선에서는 실제로 UI 스레드로
      넘어가는 지점이 없다 — 정식 `Dispatcher` 마샬링은 ViewModel이 `EventReceived`를 구독하게 될 Phase 12에서
      실제로 발생한다(development_plan.md P9-2 완료 결과 참고)
- [x] `Reader_OpenPort`/`Reader_ClosePort`/`Reader_SendCommand(0x60)` 왕복 — 응답 `0x70`의 업무 응답코드
      (`data` 첫 2byte, ASCII)를 읽어 성공/실패 판정. `baudRate`는 `115200` 고정, `pinpadCallback`은
      `nullptr`(리더기 전용 연동 — PRD §2.2.1, §10)
- [x] 하드웨어 부재 시 경로도 확인: 존재하지 않는 COM 포트로 `Reader_OpenPort` → `READER_ERR_PORT_NOT_FOUND`가
      예외 없이 반환되는지(시그니처 오류가 있으면 여기서 크래시로 드러난다)

**완료 기준**: 실장비에서 `0x60`→`0x70` 왕복 성공을 확인한다. **실장비가 없으면 그 사실을 명시하고**, 포트 부재
오류 경로까지만 검증한 뒤 하드웨어 검증을 미완료로 남긴다(검증하지 않은 것을 완료로 표시하지 않는다).

> **완료 결과(2026-08-19)**: 실장비에서 `0x60`→`0x70` 왕복 **성공**을 확인했다 — 단, 사전 확인된 대로
> 레지스트리에 저장된 `COM1`(ACPI 레거시 포트)이 아니라 **`COM5`(FTDI USB Serial)에서 성공**했다. `COM1`은
> `Reader_OpenPort` 자체는 성공하지만 `0x60` 전송 후 `READER_EVENT_TIMEOUT`만 수신되어(응답 없음) 그 뒤에
> 실제 리더기가 물려 있지 않은 것으로 확인된다. 검증은 `Interop/ReaderSerialNative.cs`와 동일한 P/Invoke
> 선언, 그리고 실제 프로덕션 `Services/Reader/ReaderService`를 그대로 호출하는 임시 콘솔 하네스(저장소 밖
> 스크래치패드, 검증 후 삭제) 2단계로 수행했다. **레지스트리는 건드리지 않았다**(`COMPORT1_FIELD`는 여전히
> `"COM 01"`) — 리더기 설정 화면에서 COM5를 선택할 수 있게 할지는 사용자 확인이 필요하다(상세는
> `development_plan.md` P9-3 참고). 리더기 설정 화면의 "초기화" 버튼은 이 왕복 경로에 임시로 연결됐지만,
> 자동화 도구 부재로 **버튼 클릭 자체는 시연하지 못했다**(레지스트리가 COM1인 채로는 버튼을 눌러도
> Timeout이 날 것으로 예상됨) — Phase 12에서 정식 배선·문구와 함께 재확인 필요.
>
> **조건부 완료 처리(2026-08-19, 사용자 확정)**: 사용자가 실제 리더기가 COM5에 연결되어 있음을 확인했다.
> 다만 리더기 설정 화면 콤보가 아직 `"COM 01"`/`"미사용"` 하드코딩 스텁이라(실제 COM 포트 열거는 Phase 12
> 몫) 화면에서 COM5를 선택할 수 없어 버튼 클릭을 통한 E2E는 구조적으로 이번 Phase에서 불가능하다. **`Services/
> Reader/ReaderService` 코드 레벨로는 실장비 왕복이 이미 검증됐으므로 Phase 9는 이 조건으로 완료 처리**하고,
> 화면 버튼을 통한 E2E 검증은 **Phase 12의 완료 조건에 포함**시켜 그때 마저 확인한다(`development_plan.md`
> P9-3 참고).

---

## Phase 10 — Reader 서비스 계층 (명령 4종 + Protocol 분리 + 안전장치)

**목표**: 결제/설정 화면 양쪽이 공용으로 쓸 리더기 제어 계층을 완성한다.

- [x] 명령 확장: `0x61`(상태체크)/`0x62`(무결성)/`0x2B`(카드 리딩)와 각 응답 `0x71`/`0x72`/`0x3B`
- [x] `Protocol/Reader/` — 요청 Data 빌더와 응답 파서를 서비스에서 분리한다.
      - `0x71` 파싱: 리더기 인증 식별번호, 모듈 ID (PRD §4.2, §6.2)
      - `0x2B` 요청: 거래구분(`ARQo`/`F`) 등 — 필드 구성은 `vendor/ReaderSerial/CSharpSample` 및 SPEC 확인
        (`reader-pinpad-spec-expert` 위임, **추측 금지**)
      - `0x3B` 파싱: 업무 응답코드(`00`/`07`/`12`/그 외)와 VAN 요청에 필요한 카드 데이터
- [x] `Services/Reader/ReaderService.cs` — **포트별 인스턴스**로 설계한다(리더기 2대 이중화 전제, PRD §2.2.3).
      포트 열기/닫기, 명령 송신, 콜백 정규화(이벤트/결과 객체로 변환)
- [x] 재연결 래퍼(`SendCommandSafe` 패턴, PRD §2.2.4 — 참조 구현
      `vendor/ReaderSerial/CSharpSample/MainForm.cs`): `PORT_NOT_OPEN`만 Close→Open→**1회** 재시도,
      `SEND_FAIL`은 방어적 `0x60` 후 원래 오류 반환, `BUSY`는 복구 대상 아님, 재오픈 시 **새 `readerId`로
      덮어쓰기**. `Reader_IsPortOpen`을 사전 게이트로 쓰지 않는다 — **구현 완료. `readerId` 없음/
      `BUSY`/`PORT_NOT_OPEN` 3가지 경로 모두 실장비로 검증됨**(`BUSY`는 실장비 2대 반복 전송 중,
      `PORT_NOT_OPEN`은 코디네이터가 포트를 연 채로 유지하며 실시간으로 케이블 분리를 조율해
      재현·재연결 복구까지 확인). `SEND_FAIL` 1가지만 저수준 드라이버 결함 주입이 필요해 API
      호출만으로 유도되지 않아 미검증으로 남는다(development_plan.md P10-3 "완료 결과(최종·2차)" 참고)
- [x] **단일 유효 응답 게이트** (PRD §8.2) — 이 프로젝트에서 가장 중요한 구조물이다. 다음 셋을 **하나의
      메커니즘**으로 처리하도록 설계한다(따로 만들면 나중에 서로 어긋난다):
      - 같은 요청에 콜백이 중복 도착 → 최초 1건만
      - **N개 리더기에 동시 전송 → 먼저 최종 응답한 1개만 채택, 나머지는 `0x60` 무효화** (PRD §2.2.3)
      - 취소/Timeout과의 경합 (Phase 16에서 이 게이트를 그대로 확장)

      N=1이 자연스러운 축약 사례가 되도록 만든다 — 단일 리더기용 코드와 이중화용 코드를 따로 두지 않는다
- [x] 페일오버 전송 — **참조 구현을 따른다**: `vendor/ReaderSerial/MfcSample/ReaderSerialTestUIDlg.cpp`의
      `BroadcastFailover()` (원본 프로젝트에서 실장비 검증 완료). 새로 설계하지 않는다
- [x] 실패 원인 구분 (PRD §4.6/§4.7, §6.6): "전문 응답코드에 의한 실패"와 "DLL 연동 실패"를 **호출자가 구분할
      수 있는 형태**로 반환한다(둘을 같은 예외/문자열로 뭉치지 않는다)

**완료 기준**: 4종 명령의 요청 생성·응답 파싱이 검증되고, 중복 콜백 주입 시 1건만 처리됨을 확인한다.
리더기 2대에 동시 전송했을 때 먼저 응답한 쪽만 채택되고 나머지에 `0x60`이 나가는 것을 확인한다(실장비가
2대 없으면 그 사실을 명시하고 1대 + 모의 응답으로 게이트 동작만 검증). `Services/`가 바이트 오프셋을 직접
다루지 않는지(계층 규칙) 점검한다.

> **완료 결과(2026-08-19)**: 코드는 전부 구현·빌드 확인(경고 0/오류 0)됐고, 프로덕션 코드를 그대로
> 호출하는 리플렉션 기반 콘솔 하네스(스크래치패드, 저장소 밖, `InternalsVisibleTo` 등 저장소 변경
> 없음)로 다음을 실제로 검증했다 — 4종 응답 파서의 정상/비정상 입력(0x3B는 22필드 전체 라운드트립
> 포함), 0x2B 요청 빌더의 `ARQo`/`F` 길이 접두 및 전체 바이트 길이(157/154byte, 손계산과 일치),
> 단일 유효 응답 게이트의 중복 콜백 dedup·무관 이벤트 무시·RECEIVE_ERROR 매칭.
>
> **1차 검증(실장비 1대 COM5 + 가짜 참여자 1개)**에 이어, **사용자가 확인해준 실장비 2대
> (COM5+COM3)로 `CardReadBroadcaster`를 3라운드 반복 실행해 재검증**했다 — 매 라운드 정확히 1개만
> 채택되고 나머지에 실제로 `0x60`이 전송됨을 로그로 확인했다(응답은 `READER_OK` 또는, 직전 라운드의
> 무효화 처리가 아직 끝나지 않아 `READER_ERR_COMMAND_NOT_ALLOWED`가 돌아온 경우도 있었으나
> fire-and-forget 설계상 예외 없이 안전하게 처리됨). 그 과정에서 `SendCommandSafe`의
> `READER_ERR_BUSY` 경로도 실장비에서 실제로 재현되어 검증됐다(`PORT_NOT_OPEN`/`SEND_FAIL`은 여전히
> 물리적 케이블 분리가 필요해 미검증).
>
> **최종 검증(2026-08-19)**: 사용자가 실시간으로 COM5/COM3 리더기에 카드를 직접 태그해 "실제 카드
> 리딩 응답코드 00(정상) 두 개가 경쟁"하는 핵심 업무 시나리오까지 확인했다(타임아웃 45초의 전용
> 하네스). **COM5가 진짜 `0x3B` 응답코드 "00"으로 채택됐고, `CardReadResponseParser`가 실제
> 하드웨어 응답에서 카드번호/모듈ID/리더기 인증 식별번호 22개 필드를 정상 파싱했으며, 아직 대기
> 중이던 COM3에 실제 `0x60`이 전송되어 `READER_OK`로 정상 접수됐다.** 곧바로 재전송한 2라운드에서는
> 방금 무효화된 COM3가 `READER_ERR_BUSY`로 즉시 완료돼 채택됐고, 1라운드의 실제 카드 데이터가
> 2라운드로 전혀 새어 들어가지 않음(라운드별 독립 종결)도 함께 확인했다.
>
> **케이블 분리 재시도, 1차(2026-08-19)**: 사용자가 실제로 COM5 케이블을 물리적으로 뽑아
> `READER_ERR_PORT_NOT_OPEN`/`READER_ERR_SEND_FAIL` 재현을 시도했다. 다만 서브에이전트와의 비동기
> 메시지 왕복 시차 때문에 케이블이 "이미 완전히 뽑힌 뒤" 확인하게 되어 `READER_ERR_PORT_NOT_FOUND`
> (포트 자체가 없음)만 재현됐다 — `PORT_NOT_OPEN`(포트는 알지만 닫힌 상태)과는 다른 오류다. 이때
> "성공적으로 연 포트가 통신 중에 끊겨야"만 이 오류가 난다는 구조적 이유를 실측으로 확인했다.
>
> **케이블 분리 재시도, 2차·최종(2026-08-19, 코디네이터 직접 실시간 조율)**: 1차에서 확인한 구조적
> 이유에 따라, 서브에이전트 위임 대신 **코디네이터가 직접** 32bit PowerShell 리플렉션 하네스(프로덕션
> `ReaderService` 그대로 호출)로 한 스크립트 안에서 `OpenPort` 성공 확인 → 75초 대기(그 사이 사용자가
> 실시간으로 케이블 분리) → 전송 시도 순서로 진행해 **`READER_ERR_PORT_NOT_OPEN`을 실제로 재현**했다.
> 로그에 `PORT_NOT_OPEN 감지 → Close 후 재연결 시도 → 재연결 실패(케이블이 빠진 상태) → readerId
> 초기화` 순서가 정확히 남았고, **사용자가 케이블을 다시 꽂은 뒤 새 `OpenPort` → 정상 왕복까지
> 성공**해 재연결 복구도 함께 확인했다. `SEND_FAIL`은 저수준 드라이버 결함 주입이 필요해 API
> 호출만으로는 유도할 수 없어 이번에도 시도하지 않았다(무리하게 진행하지 않음) — **Phase 10에서
> 실장비로 검증 가능한 범위는 모두 끝났고, 남은 `SEND_FAIL` 1가지만 저수준 드라이버 결함 주입이
> 있어야 재현 가능하다는 구조적 이유가 확인된 상태로 남는다.** 상세는 `development_plan.md`
> Phase 10 각 Task의 "완료 결과"/"완료 결과(최종·2차)" 참고.

---

## Phase 11 — 로컬 DB (SQLite) 무결성 체크 이력

**목표**: PRD §7의 저장/조회를 Phase 12·15가 쓸 수 있는 형태로 완성한다.

- [x] SQLite 도입 — `net48`/x86에서 동작하는 패키지 선정(네이티브 인터롭이 있으므로 **x86에서 실제 로드되는지**
      확인 필요. Phase 8의 x86 전환과 충돌하지 않을 것)
- [x] 스키마: 체크 일시 / COM Port / 결과 / 응답코드 / 모듈 ID / 리더기 인증 식별번호 / POS 식별번호
      (기본값 `KFTCTAXGIROCAP01`, PRD §2.1)
- [x] 저장 API + 조회 API 2종:
      - 리스트 표시용(조회기간 필터 — 리더기 설정 화면 §4.6)
      - **금일·동일 COM Port 성공 이력 존재 여부** (결제 선행 판정용, PRD §4.2)
- [x] DB 파일 위치/생성 시점 결정, 최초 실행 시 자동 생성. DB 오류가 앱을 죽이지 않을 것(PRD §9)
- [x] `Models/IntegrityCheckRow.cs`(1차 범위에서 더미용으로 만들어 둔 모델)를 실제 조회 결과에 연결
      (조회 API가 이 모델을 직접 반환하도록 연결했다 — `ReaderSetupViewModel`의 더미 데이터 배선
      교체 자체는 화면 작업인 Phase 12 몫)

**완료 기준**: 저장 → 조회 왕복이 확인되고, "금일 성공 이력" 판정이 날짜 경계/포트 구분에서 올바르게 동작한다.

> **완료 결과(2026-08-20)**: `Microsoft.Data.Sqlite` 10.0.11을 1차 시도에서 채택했다(x86/net48에서
> 네이티브 로드·연결·쿼리 성공 실측, `System.Data.SQLite.Core` 전환 불필요). `Services/Storage/
> IntegrityCheckStore`(공개 클래스, `Save`/`GetHistory`/`HasSuccessToday` 3개 API)를 신설하고,
> DB 파일은 `%LOCALAPPDATA%\KFTCTaxGiroCAP\integrity_check.db`(P8-3 `FileLogger`와 동일한 폴더
> 규칙)에 둔다. 프로덕션 코드를 그대로 참조하는 x86/net48 콘솔 하네스(`ProjectReference`로
> `KFTCOneCAP.Wpf.csproj` 참조, 스크래치패드, 저장소 밖 — 클래스가 전부 `public`이라
> `InternalsVisibleTo` 등 저장소 변경 불필요)로 저장→조회 왕복, 날짜/포트 경계값, DB 파일 손상 시
> 오류 내성(예외 없이 값으로 실패 반환)까지 전부 실측 검증했다. 실제 배포 경로를 그대로 썼지만
> 테스트 전후로 기존 파일을 백업/복원해 사용자 데이터를 건드리지 않았다. **"DB 파일 잠금"(다른
> 프로세스의 배타적 잠금) 시나리오만 코드 검토로 갈음했다** — SQLite가 `SQLITE_BUSY` 등을
> `SqliteException`으로 던지는 것은 파일 손상과 동일한 `try/catch` 경로로 흡수되므로 코드 경로가
> 같지만, 다른 프로세스로 실제 잠금을 거는 실행 검증까지는 하지 않았다. 상세는
> `development_plan.md` P11-1~P11-4 "완료 결과" 참고.

---

## Phase 12 — 리더기 설정 화면 실동작 배선

**목표**: 1차 범위에서 스텁(3초 타이머)으로 남겨둔 리더기 설정 화면을 실제 하드웨어에 연결한다. 이 Phase가
끝나면 **사람이 화면에서 직접** 리더기 제어를 확인할 수 있으므로, 결제 Flow(Phase 14~)보다 먼저 배치했다.

PRD §6(초기화/상태체크/무결성체크)에 더해, **1차 범위에서 "외부 DLL 연동 필요"를 이유로 보류된 항목들**을
여기서 함께 해소한다 — 이 항목들은 `PRD.md`에 아직 없으므로 착수 전 PRD 갱신이 필요하다
(아래 "확인 필요" 참고).

- [x] **MVVM 유지**(공통 원칙, Phase 7) — 이 Phase는 새 화면을 만들지 않고 기존 `ReaderSetupViewModel`을
      확장한다. 3초 타이머 스텁을 실제 리더기 통신으로 교체할 때도 `Services/Reader/ReaderService`(Phase 10)
      호출 결과를 ViewModel 프로퍼티에 반영하는 흐름을 유지하고, `ReaderSetupWindow.xaml.cs`에 통신 로직이나
      `x:Name` 직접 대입을 다시 들이지 않는다
- [x] 초기화(`0x60`→`0x70`, 응답 `00`) / 상태체크(`0x61`→`0x71`, 응답 `00` 또는 `08`) / 무결성체크
      (`0x61`→`0x71`→`0x62`→`0x72`, 응답 `00`) — PRD §6.1/§6.2/§6.4의 성공·실패 알림 문구 그대로
      (응답 `08` 경로는 실제 IC 카드 삽입이 필요해 실장비로 재현하지 못함, 판정 로직은 Phase 10에서
      코드 검증 완료 — `development_plan.md` P12-3 참고)
- [x] 상태체크·무결성체크 성공 시 리더기 인증 식별번호/모듈 ID 화면 출력
- [x] 무결성체크 결과를 Phase 11의 DB에 저장 + 리스트 조회를 더미에서 실제 조회로 교체
- [x] 키다운로드/업데이트는 **버튼만 유지**(동작은 PRD §11 추후 구현) — 기존 스텁 그대로 둔다
- [x] 〔1차 보류〕실제 COM 포트 열거 (`docs/home_reader_setup/PRD_WPF.md` 4.13) — `SerialPort.GetPortNames()`, "미사용" 첫 항목,
      저장된 포트가 목록에 없으면 `"<port>(사용불가)"`로 유지
- [x] COM 포트 콤보 변경 시 기존 포트를 닫고 새 포트로 다시 연다 (PRD §2.2.2 — **이 앱에서 포트를 닫는
      유일한 경우**). 그 외에는 항상 열어둔 상태를 유지한다
- [x] 로딩 스피너·동시 1작업 제한 등 기존 UI 규약(`docs/home_reader_setup/PRD_WPF.md` 4.7) 유지 — 3초 타이머만 실통신으로 교체
- [x] **Phase 9 이월 검증**: 리더기1 콤보를 실제 COM5로 선택 → 확인 저장 → 초기화 버튼 클릭 →
      `0x60`→`0x70` 왕복 성공을 화면 문구와 `FileLogger` 로그로 확인(Phase 9는 `ReaderService` 코드
      레벨로만 검증되고 화면 E2E는 이 Phase로 조건부 이월됨 — `development_plan.md` P9-3 참고)

> **이번 범위에서 제외**(PRD §6): 포트 열기 토글(`PRD_WPF.md` §4.8 — 기능 자체를 되살리지 않고 항상
> 열어두는 동작으로 확정), AOP 제약(§4.11), TRANSINFO_AOP 검증(§4.12) — 이상 2026-08-18 확정.
> **멀티패드 토글**(§4.9)은 1차 범위대로 UI에서 값을 레지스트리에 저장만 하고, 이번 범위의 DLL 연동에서는
> 그 값을 **읽어 쓰지 않는다**(2026-08-19 확정, PRD §10 — QR 기능인지 리더기+핀패드 포트 공유인지 개념이
> 확정되지 않았고 현재 PRD에 해당 요구사항이 없다).

**완료 기준**: 실장비에서 초기화/상태체크/무결성체크가 성공·실패 양쪽 모두 올바른 문구로 표시되고, 무결성
결과가 DB에 남아 리스트에 보인다. COM 포트 콤보 저장 시에만 포트가 닫히고 다시 열린다(포트 열기 토글
자체는 위 "이번 범위에서 제외" 문단대로 되살리지 않는다 — 이 문장은 착수 전 초안 당시 표현이었고 실행계획서
확정 과정에서 제외로 정리됨, 실제로는 손대지 않았다). 실장비가 없으면 그 범위를 명시하고 미완료로 남긴다.

**완료 결과(2026-08-20)**: 실장비 리더기1(COM5)/리더기2(COM3) 양쪽에서 초기화·상태체크·무결성체크
성공 경로를 전부 실측했고, 무결성체크는 DB 저장→조회 화면 왕복까지 확인했다(상세 검증 내역은
`development_plan.md` P12-1~P12-6 "완료 결과" 참고). 실패 경로(전문 응답코드 실패/DLL 연동
실패/타임아웃/`08`/1단계 실패)는 케이블 분리·카드 태그 등 물리적 개입 없이는 재현할 수 없어(코디네이터
지시로 요청하지 않음) 코드 검토로 대체했다 — DB 저장 실패만은 파일 잠금으로 물리적 개입 없이 재현해
실측했다(성공 문구가 그대로 유지됨을 확인). 코디네이터가 사용자와 직접 확인이 필요하다면: 케이블을
뽑아 DLL 연동 실패/타임아웃 문구를, IC 카드를 태그해 상태체크 `08` 성공 경로를, 0x71 실패 시
`ResponseCode`/`ModuleId`가 DB에 null로 저장되는지를 재현해 볼 수 있다.

**Opus 전체 검증 리뷰 및 후속 수정(2026-08-20)**: Sonnet 구현이 끝난 뒤 Opus가 코드 재검토 +
실장비 재현으로 별도 전체 검증을 수행해 2가지 결함(X/Alt+F4 닫기가 dirty-check·`DiscardPortChanges`를
건너뛰어 레지스트리와 실제 포트 연결이 어긋나는 문제, 리더기1/2에 같은 COM 포트를 지정해도 경고 없이
저장되는 문제)을 실장비로 확정 재현했고, Sonnet이 둘 다 수정 후 재검증까지 완료했다. 그 수정의 부수
효과(워밍업 인스턴스가 `Closing` 로직을 불필요하게 타는 문제)도 함께 정리했다 — 상세 내역은
`development_plan.md` P12-6 "수정(Opus 전체 검증 리뷰에서 발견)" 항목 참고.

---

## Phase 13 — 결제 알림창 UI

**목표**: 결제 흐름이 쓸 알림창을 **독립적으로** 완성한다(Flow 연결은 Phase 15).

- [x] **`ViewModels/PaymentNoticeViewModel` 신설**(공통 원칙, Phase 7) — 새 화면이므로 처음부터 MVVM으로
      만든다. IC/FALLBACK/통신중 3개 상태를 하나의 열거값 프로퍼티로 표현하고, `Views/PaymentNoticeWindow`는
      그 값에 바인딩해 이미지·문구를 전환한다(개별 `Visibility`를 화면마다 따로 대입하지 않는다 — Phase 7
      P7-3에서 정립한 "상태 열거값 하나에서 파생" 원칙을 그대로 따른다)
- [x] `Views/PaymentNoticeWindow` 신설 — IC/FALLBACK/통신중 3개 상태 전환 (PRD §5.2). **자산은 계획과
      달리 `BG_IMG_IC`/`BG_IMG_MS`/`BG_IMG_PROCESSING`(카드까지 합성된 배경) 대신, 리더기·화살표·카드를
      레이어로 분리한 자산(사용자가 직접 제작)으로 최종 확정됐다** — 상세는 `development_plan.md`
      "Phase 13 시각 구현 — 중간 정리(2026-08-24)" 참고.
- [x] 이미지 자산 배치 완료 — 단, 계획 당시의 `_VERYSMALL` 두 벌 체계는 폐기되고 리더기(1장, 3상태 공용)
      + 화살표(2장) + 카드(2장) 구조로 바뀌었다. `LOCK`/`QR`은 예정대로 미사용.
- [x] 항상 최상단 표시(`Topmost`) — 됨.
- [x] **취소 1회 제한 + `VanProcessing` 취소 차단** (P13-2, 2026-08-24) — `PaymentNoticeViewModel`에
      `_canceled` 플래그와 `IsCancelAllowed`(취소 가능 여부 판정 유일 지점) 도입, `CancelCommand`가
      `CanExecute`로 자동 바인딩돼 View 코드비하인드 없이 버튼 비활성화까지 처리됨. ESC 연동은 P13-5에서.
- [x] **메인 화면 비노출 정책** (P13-4, PRD §5.1, 2026-08-24) — `PaymentNoticeWindow` 생성자가
      떠 있는 `HomeWindow`를 알림창 표시 전에 자동으로 트레이로 내림(`SuppressHomeWindowForeground`).
      실기 검증: 홈 최소화 상태/떠 있는 상태 둘 다에서 알림창 표시·종료 후 홈이 복원되지 않음 확인.
- [x] **ESC 키 전역 Hooking + 해제 보장** (P13-5, PRD §5.3, 2026-08-24) — `WH_KEYBOARD_LL` 저수준
      전역 훅(`Interop/LowLevelKeyboardHookNative.cs` + `Views/PaymentNoticeEscapeHook.cs`)으로
      POS 등 다른 프로그램에 포커스가 있어도 ESC로 취소 가능. `VanProcessing` 중에는 게이트가 막아
      ESC를 삼키지 않고 그대로 흘려보냄(실기 검증 완료). 10회 연속 열고 닫아도 훅 누적/누락 없음.
- [x] **제어 진입점 계약** (P13-6, 2026-08-24) — `Services/Payment/IPaymentNoticePresenter`(WPF 타입
      없음) + `Views/PaymentNoticePresenter`(Dispatcher로 UI 스레드 마샬링). 백그라운드 스레드에서
      Show/ChangeState/Close 호출 시 예외 없음, 닫힌 뒤 호출은 조용히 무시+로그, `Canceled`는 취소
      1회당 정확히 1회 발생 — 모두 실기 검증 완료.
- [x] **9개 시나리오 검증** (P13-7, 2026-08-24) — 상태 전환/Topmost/ESC 취소·해제/버튼+ESC 연타/
      `VanProcessing` 차단/홈 표시 정책 2가지/10회 반복/백그라운드 스레드 제어까지 모두 실기 확인
      (상세 표는 `development_plan.md` P13-7). 리더기 설정 화면과 알림창이 **동시에** 떠 있는 경우는
      정책 미정으로 별도 보류(Phase 15에서 재검토, 위 Phase 15 섹션 참고).
- [ ] 크기 조절 기능은 이번 범위 밖 — 두 크기 자산만 준비해 두고 전환 트리거는 요구사항 확정 후(PRD §10)

**완료 기준**: 3개 화면 전환·Topmost·ESC=취소가 동작하고, 창을 닫으면 ESC 후킹이 해제된 것을 확인한다.
**→ 충족(2026-08-24). Phase 13 전체 완료**(시각 정교화는 별도 재개 예정, 리더기 설정 화면 동시 표시
정책은 Phase 15 재검토 대상 — 완료 기준 자체와는 무관).

**실행계획서 작성 완료(2026-08-20)**: `development_plan.md` P13-1~P13-7. 이미지 자산 6장을 직접 열어
확인한 결과와 그에 따른 사용자 확정 3건이 반영돼 있다 — ① 이미지에 취소 버튼이 그려져 있지 않아
**하단 중앙 오버레이**로 얹는다, ② ESC는 **전역 저수준 훅**(`WH_KEYBOARD_LL`)으로 잡는다(POS에 포커스가
있어도 동작해야 하므로 창 `KeyDown`으로는 부족), ③ 배경이 애니메이션 GIF로 바뀔 가능성은 **구조만**
확보하고(배경 소스 생성 지점 1곳) 패키지·코드는 넣지 않는다. 위 항목에 더해 **Phase 15가 쓸 제어 진입점
계약**(`IPaymentNoticePresenter` — 워커 스레드에서 안전하게 호출)도 이 Phase 범위에 포함시켰다.

**시각 구현 중간 정리(2026-08-24, 사용자 지시로 여기까지 진행 후 일시 중단)**: 계획 당시 상정했던
"카드까지 합성된 배경 이미지 + 벡터 카드" 대신, 리더기/화살표/카드를 레이어로 분리하고 카드는 사용자가
직접 만든 실사 이미지로 교체하는 방향으로 실제 구현이 크게 달라졌다. 카드 애니메이션(IC/MS 둘 다 "바깥
→ 슬롯 앞 도달 시 정지 → 유지 → 리셋" 구조로 통일, MS는 화살표 실측 각도 31.4도와 일치)과 PROCESSING
궤도 글로우 애니메이션(타원 뒤쪽 절반이 몸통 위에 비쳐 보이는 기하 버그를 두 차례 발견해 수정) 모두
여러 차례 실측·재검토를 거쳤다 — 상세 경위와 다음에 참고할 시행착오 기록은 `development_plan.md`
"Phase 13 시각 구현 — 중간 정리(2026-08-24)" 참고. **시각 정교화(추가 폴리싱)는 사용자 지시로 일단
여기서 멈추고 추후 재개**하며, 대신 **실제 결제 로직에 필요한 P13-2부터 이어서 구현**하기로 함
(2026-08-24). **P13-2~P13-7이 모두 완료돼 Phase 13 전체가 끝났다**(위 체크리스트) — 시각 정교화만
사용자 지시로 별도 재개 예정으로 남아 있다.

**Opus 전체 검증 리뷰 및 후속 수정(2026-08-24)**: Sonnet 구현이 끝난 뒤 Opus가 코드 재검토 + 실측
검증으로 별도 전체 검증을 수행해 결함 5건(ESC 삼킴/취소 확정 경쟁 상태 — 결제 안전성 직결로 가장
심각, 데모 타이머 누수, 기본 실행 진입점이 데모 창이던 문제, 전역 훅 설치/해제 비대칭, Presenter
재사용 시 취소 플래그가 다음 거래로 새는 문제)을 확정했고, Sonnet이 전부 수정 후 재검증까지 완료했다
— Phase 12(P12-6)와 같은 워크플로우. 상세 내역은 `development_plan.md` "Phase 13 — Opus 전체 검증
리뷰 및 후속 수정(2026-08-24)" 참고.

---

## Phase 14 — 소켓 서버 + 단일 워커 Queue

**목표**: POS 요청을 받아 순차 처리하는 골격을 만든다(결제 내용은 아직 비어 있어도 됨).

- [x] `localhost:8002` 소켓 서버 기동/종료 (앱 수명주기에 연결, PRD §3.1)
- [x] `Protocol/Pos/` — **임시 테스트 전문** 정의(실제 SPEC 확정 시 이 폴더만 교체, PRD §10) —
      길이-접두 방식(`[길이4][본문]`, STX/ETX 없음)으로 확정
- [x] 단일 워커 + Queue (PRD §3.2, §8.1): 거래는 한 번에 하나, 처리 중 도착한 요청은 **수신 순서대로** 대기
- [x] 소켓 오류가 앱 전체를 죽이지 않을 것 (PRD §9) — 연결 단절/잘못된 전문 처리
- [x] 검증용 테스트 클라이언트(개발용, 최종 산출물 아님) — `--pos-client-test`

**완료 기준**: 요청 3건을 동시에 밀어 넣으면 정확히 순차로 1건씩 처리되고, 순서가 보존된다. 클라이언트가
중간에 끊어도 서버가 계속 살아 있다. **→ 충족(2026-08-24)**. 상세 근거는 `development_plan.md`
Phase 14 각 Task의 "완료 조건" 절 참고.

**Opus 전체 검증 리뷰 및 후속 수정(2026-08-24)**: Sonnet 구현이 끝난 뒤 Opus가 코드 재검토로 별도 전체
검증을 수행해 결함 2건(H-1 워커 스레드가 소켓 쓰기까지 맡아 응답 전송이 막히면 전체 큐가 멈춤, M-1
AcceptLoop가 진짜 오류와 정상 종료를 구분하지 않아 로그 없이 조용히 죽음)을 확정했고, Sonnet이 전부
수정 후 재검증까지 완료했다 — Phase 12(P12-6)/13과 같은 워크플로우. 상세 내역은 `development_plan.md`
"Phase 14 — Opus 전체 검증 리뷰 및 후속 수정(2026-08-24)" 참고.

---

## Phase 15 — 결제 Flow 조립

**목표**: Phase 10(리더기)·11(DB)·13(알림창)·14(Queue)을 엮어 PRD §4.1의 순서를 완성한다.

- [x] `Services/Payment/PaymentOrchestrator` — PRD §4.1 7단계 순서 구현
- [x] 무결성 선행 판정 (PRD §4.2): 설정된 **리더기 각각** 금일 성공 이력 확인 → 없으면 그 포트만 무결성 체크
      → **성공한 리더기만** 카드 리딩에 참여. 양쪽 다 실패해야 거래 오류
- [x] IC 정상 (PRD §4.3): 알림창 IC → **참여 리더기 전체에** `0x2B`(거래구분 `ARQo`, 동일 명령·동일 필드)
      동시 전송 → 먼저 최종 응답한 쪽 채택 + 나머지 `0x60` 무효화 → 응답 `00`이면 VAN 요청 데이터 생성
- [x] FALLBACK (PRD §4.4): `0x3B` 응답 `07` → 알림창 MS로 전환 → **채택된 그 리더기에만** `0x2B`
      재요청(거래구분 `F`) — 양쪽에 다시 뿌리지 않는다
- [x] 응답 `12` (PRD §4.5): **채택된 그 리더기에만** `0x2B` 재요청(거래구분 `ARQo` 유지)
- [x] 그 외 응답 (PRD §4.6): 리딩 실패 → 리더기 초기화(`0x60`) → POS에 오류·원인 응답
- [x] DLL 연동 실패 (PRD §4.7): 응답코드 실패와 **구분**해 POS에 응답
- [x] 포트 사용 불가 처리 (PRD §2.2.3): 양쪽 모두 `"미사용"`이면 카드 리딩을 시도하지 않고 즉시 오류 전문
      반환. 포트 **열기 실패**는 이와 구분해 §2.2.4 재시도 래퍼에 맡긴다
- [x] 거래 종료 시 카드 데이터 즉시 삭제 + 이전 거래 잔여 콜백이 다음 거래에 영향 없음 (PRD §8.4) —
      이중화에서는 무효화된 리더기의 뒤늦은 응답도 여기에 포함된다
- [x] **경계**: VAN 단계는 **스텁 + `PROCESSING` 전환까지만**(실제 `FNAISCRDVAN`은 Phase 20 — 이 항목을 쓸
      당시 번호로는 17), 취소/Timeout은
      **단순 배선까지만**(경합 4종의 단일 결과 확정 게이트는 Phase 16) — 2026-08-25 사용자 확정
- [x] **★ 확정(2026-08-25, P13-4에서 보류됐던 항목)**: 사용자가 `ReaderSetupWindow`(리더기 설정, 모달)를
      연 채로 결제 요청이 들어오면 **카드 리딩을 시도하지 않고 즉시 오류 전문을 반환**한다(설정 화면을 강제로
      닫지 않는다). 같은 COM 포트를 설정 화면과 결제 워커가 동시에 쓰는 상황 자체를 만들지 않는 것이 오류
      소지가 가장 적다는 판단. 반대 방향(거래 진행 중에 설정 화면을 여는 것)을 UI에서 막을지는 **Phase 16
      착수 시 확정**한다. 상세는 `development_plan.md` P15-4.

**완료 기준**: 정상/FALLBACK/`12` 재시도/기타 실패/DLL 오류 5개 시나리오가 각각 올바른 분기로 끝나고, POS에
구분 가능한 결과가 반환된다. 리더기 2대 구성에서 한쪽으로 카드를 넣었을 때 반대쪽이 `0x60`으로 정리되는 것,
한쪽만 설정된 구성에서도 동일하게 동작하는 것(N=1)을 확인한다. 연속 2건 거래 시 앞 거래 데이터·잔여 콜백이
뒤 거래에 섞이지 않는다. **→ 충족(2026-08-25)** — 가짜 엔드포인트 기반 16개 시나리오(`--payment-flow-test`)
전부 통과, 실제 Orchestrator 경로는 `--pos-client-test` 재실행으로 회귀 확인. **추가로 실제 리더기(COM5)
연결 상태에서 정상 IC 카드 리딩→VAN 승인까지 실장비로 실증**(설정 화면 게이트 실전 거부, COM03 실패 시
N=1 축소 동작, 알림창 실제 표시/자동 닫힘까지 전부 확인). 상세는 `development_plan.md` P15-6~P15-10
및 체크포인트 1/2 Opus 검증 리뷰 절 참고.

---

## Phase 16 — 사용자 취소 & Timeout 동시성 확정

**목표**: PRD §8.3의 경합 4종에서 **결과가 정확히 하나만** 확정되게 한다.

Phase 15까지는 정상 흐름 위주이므로, 경합 처리를 별도 Phase로 분리해 집중 검증한다(이 프로젝트에서 가장
버그가 나기 쉬운 지점).

- [x] 사용자 취소 (PRD §4.8): 취소 버튼/ESC → **응답 대기 중인 모든 리더기**에 초기화(`0x60`) → POS에 취소
      응답 (이중화에서 한쪽만 정리하면 나머지가 계속 카드를 기다린다)
- [x] 카드 입력 Timeout (PRD §4.9): **거래 단위 데드라인 1개**를 정본으로 삼는다 → **대기 중인 모든 리더기**
      초기화 → POS에 Timeout 응답
- [x] **★ 확정(2026-08-25)**: Timeout은 **거래 단위로 하나만** 잰다 — 라운드마다 120초를 새로 주지 않는다
      (그러면 FALLBACK·`12`가 겹칠 때 POS가 최악 360초를 기다린다). 시작 120초 + **새 사용자 입력 단계가
      시작될 때마다 +30초** 연장(현재는 `07`/`12` 재요청, 추후 서명·PIN 입력도 같은 규칙). 연장을
      "07/12 전용 예외"가 아니라 **일반 규칙**으로 구현한다. 상세는 PRD §4.9
- [x] 단일 결과 확정 게이트 — 다음 경합에서 1건만 처리: 카드리딩 완료+취소 / 카드리딩 완료+Timeout /
      취소+Timeout / 콜백 중복 / **두 리더기가 거의 동시에 응답**. **경합 시 먼저 확정된 쪽이 이긴다**
      (선착순, 2026-08-25 사용자 확정 — 특정 결과에 우선권을 주지 않는다)
- [x] 거래 종료 시 타이머·콜백·ESC 후킹 등 리소스 해제 (PRD §9)
- [x] **★ 확정(2026-08-25, Phase 15에서 이월된 항목, 2026-08-26 실기 확인 후 수정)**: 거래가 진행 중일 때
      사용자가 리더기 설정 화면을 열려고 하면 **열지 않는다**. 처음엔 안내 메시지도 계획했으나, 실기로
      확인해 보니 결제 알림창이 Topmost라 그 뒤에 가려져 사용자가 안내를 볼 수 없었다 — 안내 없이
      **조용히 무시**하는 것으로 수정(결제 알림창이 이미 최상단에 떠 있어 "결제 중"임을 스스로 보여줌).
      Phase 15가 만든 반대 방향 게이트(설정 화면 열림 중 결제 요청 → 즉시 거부)와 짝을 이뤄 COM 포트
      경합을 양방향 모두 차단한다. 실사용에서는 홈 화면 카드 클릭이 Topmost 알림창에 가려 물리적으로
      불가능하므로 **트레이 우클릭 메뉴가 사실상 유일한 접근 경로**임을 실기로 확인. 상세는 PRD §6

**완료 기준**: 경합 5종을 인위적으로 재현해 각각 결과가 1건만 확정되고 나머지는 무시되는 것을 확인한다.
취소/Timeout 시 대기 중이던 리더기가 **모두** 정리되는 것을 확인한다. 반복 실행 시 타이머/후킹이 누적되지
않는다. 데드라인이 거래 단위로 1개만 돌고 `07`/`12` 재요청 시 +30초 연장되는 것을 로그로 확인한다.

**완료(2026-08-26)**: 위 항목 전부 구현·검증 완료. Opus 검증 리뷰(체크포인트 1)에서 결함 1건(예외
경로에서 **다음 거래의 리더기**에 `0x60`이 발사될 수 있던 문제)과 개선 4건을 발견해 전부 수정했다.
`--payment-flow-test` 시나리오 1~26(경합 9종 + H-1 회귀 방지 1종 포함) 전부 통과, Phase 14 소켓 회귀
7개 흐름 정상. 실장비(COM5) 검증: 정상 승인 / 취소 즉시 반응(응답까지 3.5초, 대부분 사람 반응 시간) /
`07` 발생 시 데드라인 정확히 +30초 연장(120→145.9초 실측) / 데드라인 정확히 120초에 만료 / 반대 방향
게이트(설정 화면 열림 중 결제 요청 즉시 거부, 0.01초) — 전부 실기 재현. "VAN 진입 후 ESC 무시"는
UI 레벨(Phase 13에서 이미 검증) + 게이트 레벨(가짜 하네스 시나리오 24)로 나눠 이미 검증됐음을 확인,
추가 실장비 시도는 새로운 것을 증명하지 못해 보류. 이후 리더기 2대(COM5+COM3/COM7)를 실제로 동시 연결한
상태로 P16-3의 "2대 모두 0x60" 조건도 실물 재검증: 정상 경쟁(먼저 응답한 쪽 채택·반대쪽 무효화), 취소
2대 모두 정리, Timeout 2대 모두 정리(119.97초 실측)까지 전부 통과 — 도중 리더기 자체 내장 타임아웃(약
60초, `04` 응답)이 소프트웨어 데드라인보다 먼저 개입한 사례도 발견했고 정상 처리됨을 확인. P16-5(설정
화면 차단)는 실기 확인 중 안내 메시지가 Topmost 알림창에 가려지는 문제를 발견해 "조용한 무시"로 수정 후
재검증 완료. 상세는 `development_plan.md`의 "Phase 16 P16-1~P16-6 구현 및 검증 기록", "Phase 16 체크포인트
1 — Opus 검증 리뷰", "Phase 16 실장비(실제 리더기) 검증 기록", "Phase 16 실장비 2대(리더기 1/2 동시 연결)
검증", "P16-5 실기 확인 결과 및 후속 수정".

---

## Phase 17 — POS↔원캡 실제 전문 적용 (SPEC 반영)

**목표**: 임시 테스트 전문(`PAY|금액|거래ID`)을 실제 SPEC 전문 3종으로 교체하고, `PaymentOrchestrator`를
"요청 1건 = 결제 1건" 전제에서 **전문 종별 분기 구조**로 재구성한다.

**근거 문서**: `spec/국세 베리어프리 키오스크용 전산설계서(POS-원캡)_20260826.pdf`(전 18페이지, PDF가 유일하게
파싱 가능 — 같은 폴더 `.hwp`는 DRM 배포용 문서 래퍼라 열리지 않는다). **필드를 코드에 하드코딩하기 전에 반드시
`pos-onecap-spec-expert` 서브에이전트로 확인한다** — 표를 눈으로 훑으면 SET 장소 열을 착각하기 쉽다(실제로
최초 정리 때 `#48`을 kiosk로 잘못 읽은 전례가 있다).

### 전문 3종의 성격이 서로 다르다

| 전문 | 거래구분 | 본문 길이 | 카드리딩 | 원캡이 채우는 필드 | 알림창 |
|---|---|---|---|---|---|
| 국고 상세 고지내역 조회 | `501008` | 706 | 없음 | **없음**(순수 중계) | 통신중부터 |
| 카드 정보 조회 | `800000` | 500 | 필요 | `#14 BIN`(AN 8) 1개 | IC → 통신중 |
| 국고 신용카드 승인 요청 | `902614` | **1500**(계산값) | 필요 | 7개(아래) | IC → PIN → 통신중 |

`902614`에서 원캡이 채우는 7개 필드: `#43` 보안단말기 인증번호(ANS 32) / `#44` FALLBACK CODE(N 2) /
`#45` 복호화 정보(AN 18) / `#46` 암호화된 카드정보(AN 196) / `#48` 거래 입력 유형(AN 1, 2:Swipe·4:Pay-On·5:IC) /
`#50` 신용카드 승인 인증방식(AN 1, 고정값 `"2"`) / `#53` EMV DATA(ANS 604).
여기에 **`#51` 암호화된 비밀번호 정보(ANS 100)가 추가된다** — 설계서에는 kiosk로 표시돼 있으나 설계서 오류이며
원캡 담당이다(Phase 18). 이번 Phase에서는 **space 스텁**으로 둔다.

> `902614`만 **총 길이 행이 SPEC에 없다**(501008=706, 800000=500은 명시). 마지막 필드 `#54`(AN 172,
> POSITION 1328)로부터 1328+172=1500으로 계산한 값이므로, 구현 시 실제 전문으로 재확인한다.

### 확정 사항 (2026-08-26 사용자 확정)

- **`#0 전문 길이`에는 본문 길이를 넣는다** — 706/500/1500. 길이 4byte는 본문 밖 헤더이므로 소켓에 실제로
  나가는 바이트는 710/504/1504다(`#1 업무구분`이 POSITION 0인 것이 근거).
- **전문마다 TCP 연결을 새로 연다** — 3전문은 하나의 거래로 묶이지 않는 **각각 독립한 기능**이다.
  따라서 `800000`에서 읽은 카드 데이터를 `902614`로 넘기지 않으며, **사용자는 카드를 2회 댄다**
  (PRD §8.4 "거래 종료 시 카드 데이터 즉시 삭제"를 전문 단위로 그대로 유지).
- **카드리딩 로직은 3전문에서 동일하다** — `800000`도 `902614`와 똑같이 거래구분 `ARQo`로 요청하고,
  응답의 카드번호 필드에서 **앞 8자리(BIN)만 파싱**해 채운다. FALLBACK(`07`)·`12` 재요청 흐름도 양쪽 모두
  기존과 동일하게 존재한다.
- **`#43` 보안단말기 인증번호** = 리더기 식별자(16) + 프로그램 식별자(16). 리더기 식별자는 **카드리딩 응답의
  "리더기 인증 식별 번호"**를 쓰고, 프로그램 식별자는 **`KFTCTAXGIROCAP01`(정확히 16자)**을 상수 1곳에
  선언해 쉽게 바꿀 수 있게 둔다.
- **`#38` 카드소유주 주민(사업자)등록번호는 항상 공백**이며 kiosk가 채운다(SPEC 표에는 SET 장소 ○가 없고
  p.15 설명 절에는 포함돼 있어 문서가 어긋나 있었던 항목 — 이 결정으로 해소).
- **원캡 자체 오류 응답코드 체계를 신설한다.** SPEC 3장은 인터넷지로/KFTCVAN이 내려주는 코드만 정의하고
  원캡이 스스로 실패시키는 경우(취소·Timeout·리더기 실패·DLL 오류)의 코드가 없다. SPEC이 이미 `M01`/`V01`처럼
  **주체를 접두 문자로 구분**하는 관례를 쓰고 있으므로 이를 따르되, 이미 쓰인 `M`/`V`는 피한다:

  | 접두 | 주체 | 예 |
  |---|---|---|
  | `E` | 원캡 자체 판단 + 전문 오류 | `E01` 사용자 취소, `E02` 카드입력 Timeout, `E03` 설정화면 열림, `E04` 리더기 미설정, `E40` 전문 길이 불일치, `E41` 알 수 없는 거래구분 |
  | `R` | 리더기 관련 | `R0x` 카드리딩 업무 응답코드 실패, `R2x` 리더기 DLL 연동 실패(`PORT_NOT_OPEN`/`SEND_FAIL`/`BUSY` 각각 구분) |
  | `D` | VAN DLL(`KFTC_GIRO`) | `D01` DLL 로드 실패, `D02` 호출 실패(`nRet == -1`) |

  (2026-08-26 정정: 원래 `G`를 배정했으나, `G`도 VAN이 실제로 내려줄 수 있는 응답코드 대역이라 충돌
  위험이 있어 `D`로 변경. 배제 대역은 `A`/`B`/`G`/`H`/`Z`(VAN·카드사 응답코드가 쓸 수 있음) — 우리 코드는
  `E`/`R`/`D`만 쓴다.)

  이로써 기존 2자리 POS 결과코드는 사라지고, "DLL 실패 원인을 더 잘게 나누고 싶다"던 요구도 `R2x`/`D0x`에서
  함께 해결된다. (2026-08-31 정정: 이 문단이 위 표에서 이미 `D`로 정정된 뒤에도 `G0x`로 남아 있던 오기 —
  P19-8 문서 갱신 때 발견해 정정.)

### 작업 항목

- [x] `Protocol/Pos/` 전면 교체 — 고정길이 프레이밍(`#0` 4byte 헤더 + 본문), 공통부 14필드, 전문별 필드
      테이블(번호/표현/길이/POSITION). **"kiosk가 채우지 않는 필드는 space로 채워 총 길이를 유지"** 규칙을
      빌더의 기본 동작으로 둔다(SPEC p.5 각주)
- [x] 전문 종별 라우팅 — 수신 전문의 `#4 거래 구분 코드`(N 6, POSITION 10)로 501008/800000/902614 분기.
      알 수 없는 코드는 `E41`
- [x] `501008` 경로 — 카드리딩 없이 **알림창을 곧바로 통신중 상태로 띄우고** VAN 중계만 한다(실제 VAN 연결은
      Phase 20, 그전까지는 스텁)
- [x] `800000` 경로 — 알림창 IC → 카드리딩(`ARQo`) → 응답 카드번호에서 BIN 8자리 파싱 → `#14`에 채워 중계 →
      통신중
- [x] `902614` 경로 — 알림창 IC → 카드리딩(`ARQo`) → 7개 필드 채움 → (`#51`은 이번 Phase에서 space 스텁) →
      통신중. Phase 15/16이 만든 FALLBACK/`12`/취소/Timeout 흐름이 여기에 그대로 붙는다
- [x] 응답코드 체계 교체 — 기존 2자리 `PosPaymentResultCode`를 SPEC 3자리 + 위 `E`/`R`/`D` 체계로 대체하고,
      `#7 응답 코드`(AN 3, POSITION 20)에 실어 보낸다
- [x] 큐/워커 정책 재확인 — 전문이 독립 기능이므로 단일 워커 큐는 **전문 단위로 1건씩** 처리한다. 카드리딩이
      없는 `501008`에 120초 데드라인을 거는지 여부를 명시적으로 정한다
- [x] `PRD.md` 갱신 — §10의 "실제 통신 전문 미확정"을 해소하고, §3(소켓)·§4(결제 Flow) 기술을 SPEC 기준으로
      고친다. 2회 카드리딩·전문별 연결 수명도 반영

**완료 기준**: 3종 전문을 각각 정확한 길이(710/504/1504 바이트)로 만들고 파싱하는 것을 **바이트 단위로**
확인한다(필드 POSITION이 SPEC 표와 일치). 원캡이 채우는 필드가 카드리딩 결과로 정확히 채워지고 kiosk가 채운
필드는 그대로 보존된다. Phase 15/16의 분기(FALLBACK/`12`/취소/Timeout)가 `902614` 경로에서 회귀 없이 동작하고,
실패 시 `E`/`R`/`D` 코드가 구분되어 POS에 전달된다.

**완료(2026-08-27)**: 위 항목 전부 구현·검증. 체크포인트 1(전문 계층)에서 결함 3건 + 개선 1건, 최종 Opus
검증에서 결함 3건(H-1 옛 임시 전문 타입이 **모순된 옛 2자리 매핑표를 그대로 노출**, **H-2 VAN 통신 중
알림창이 사용자에게 전혀 보이지 않음** — 이번 리뷰 최대 결함, H-3 VAN 실패 시 리더기 초기화 누락)과
M-1(로그 상관관계 키가 `GetHashCode` 기반이라 POS·VAN 로그와 대조 불가)을 발견해 전부 수정·재검증했다.
가짜 하네스 24개 조건, Phase 14 소켓 회귀 7종, 3전문 바이트 단위 SPEC 대조 39개 체크포인트 전부 통과.

**실장비 검증도 완료**(COM5 + 실제 카드): 3전문 정상 처리(`800000` BIN 추출, `902614` 원캡 7필드 실측
확인), 연속 3건 무오류(**"거래 종료 시 무조건 초기화" 논의를 실증으로 종결** — 초기화 없이도 안전),
사용자 취소(`E01`), 알 수 없는 거래구분(`E41`)까지 확인. 상세는 `development_plan.md`의 "Phase 17
체크포인트 1", "Phase 17 최종 검증 — Opus 리뷰 및 후속 수정", "Phase 17 실장비 검증 기록" 참고.

**남은 주의 사항**: `#46` 암호화된 카드정보가 실측 192바이트로 196 한도에 여유가 4바이트뿐 — 다른 카드로
추가 확인 권장. 인코딩(CP949)·패딩 규칙은 발주처 확인 대기(각각 상수 1곳).

---

## Phase 18 — 카드 비밀번호 입력 (알림창 4번째 상태)

**목표**: `902614` 승인 요청에 필요한 `#51 암호화된 비밀번호 정보`를 채우기 위해, 카드리딩 성공 후 사용자에게
4자리 비밀번호를 입력받는 화면을 만든다.

> **왜 별도 Phase인가**: 기존 로드맵에 없던 새 요구사항이고(설계서의 kiosk 표시가 오류였음), Phase 13이
> 알림창을 "UI를 독립적으로 완성하고 Flow 연결은 나중에" 방식으로 처리한 것과 성격이 같다. Phase 17은
> `#51`을 space 스텁으로 두고 전문 계층만으로 완결 검증이 되므로 분리가 가능하다.

### 확정 사항 (2026-08-26 사용자 확정)

- **`902614`(승인 요청)에서만 받는다.** `800000`에는 `#51` 필드 자체가 없으므로 카드리딩만 하고 PIN은 받지
  않는다.
- **별도 창이 아니라 알림창의 4번째 상태**로 만든다(코디네이터 판단, 사용자 위임). ESC 전역 훅·`Topmost`·
  취소 게이트·홈 화면 비노출 정책을 그대로 재사용할 수 있고, Phase 13에서 세운 "상태 열거값 하나에서
  파생" 원칙도 유지된다. 레이아웃 실측상 현재 창 크기(750×650)에 들어가므로 **창 크기는 바꾸지 않는다.**
- **입력 규칙**: 4자리 마스킹이되 **누른 숫자를 잠시 보여준 뒤 마스킹**한다. **4자리를 다 누르면 자동 진행**
  (확인 버튼 없음). **한 자리씩 지우는 버튼**이 있다. **재입력 기회는 주지 않는다** — 비밀번호가 틀려
  VAN이 거절하면 그대로 POS에 실패 응답한다.
- **취소 버튼과 ESC가 그대로 동작**하며, PIN 입력 단계 진입 시 **데드라인 +30초 연장**이 적용된다. 이는
  Phase 16이 "새 사용자 입력 단계가 시작될 때마다 +30초"를 **일반 규칙**으로 만들면서 "추후 서명·PIN 입력도
  같은 규칙"이라고 미리 명시해 둔 것의 첫 실사용 사례다 — 예외 분기를 새로 만들지 않는다.
- **암호화는 나중에 확정된다.** SEED 암호화를 적용할 예정이나 방식이 미정이므로, **일단은 입력받은 4자리를
  그대로 `#51`에 채운다**(좌측정렬 + space 96자리). 암호화가 확정되면 **이 한 지점만 교체**되도록 변환을
  독립 함수로 분리해 둔다.

### 작업 항목

- [x] `PaymentNoticeState`에 PIN 입력 상태 추가 — 기존 IC/FALLBACK/PROCESSING과 같은 열거값 하나로 전환(P18-1)
- [x] 레이아웃 구현 — `Assets/Images/PaymentNotice/pin입력.png` 참조. 카드+자물쇠 아이콘, 문구 2줄(한/영),
      PIN 4칸(현재 입력 위치 강조), 3×4 키패드(1~9 / 지우기 / 0 / 빈칸), 하단 취소 버튼(P18-2)
- [x] 입력 로직 — 자릿수 표시, 눌린 숫자 잠시 노출 후 마스킹, 4자리 도달 시 자동 진행, 한 자리 삭제(P18-3)
- [x] 데드라인 +30초 연장을 **기존 일반 규칙 경로로** 호출한다(PIN 전용 예외를 만들지 않는다)(P18-4)
- [x] 취소/ESC/Timeout이 PIN 입력 중에도 단일 결과 확정 게이트(Phase 16)를 그대로 통과하는지 확인(P18-4,
      체크포인트 1에서 중복 리더기 초기화 결함 발견·수정)
- [x] `#51` 채움 — 현재는 평문 4자리 + space 96. **암호화 교체 지점을 함수 1곳으로 격리**하고 주석으로
      명시(P18-5, `Services/Payment/PinFieldEncoder.cs`)
- [x] 입력된 비밀번호를 거래 종료 시 즉시 폐기 (PRD §8.4, 카드 데이터와 동일 취급)(P18-3/P18-5, 로그
      미노출까지 검증)

**완료 기준**: `902614` 흐름에서 카드리딩 성공 후 PIN 화면이 뜨고, 4자리 입력 시 자동으로 통신중으로 넘어가
`#51`이 채워진 전문이 만들어진다. PIN 입력 중 취소/ESC/Timeout이 각각 정확히 1건만 확정되고, 데드라인이 +30초
연장된 것이 로그로 확인된다. 반복 실행 시 이전 거래의 입력값이 남지 않는다.

**완료(2026-08-28)**: P18-1~P18-7 코드 구현 + 체크포인트 1(Opus 검증 리뷰, M-1/L-1 발견·수정) + 최종
검증(H-1: 실패 응답에 PIN이 실리는 결함 발견·수정) + `--payment-flow-test` 하네스 47건 전부 통과 +
**실장비 검증 5개 시나리오 전부 통과**(902614 정상(터치/키보드)/800000 PIN 미진입/PIN 중 취소(ESC)/
PIN 중 Timeout). 실장비 검증 중 사용자가 물리 키보드 입력도 요구해 **P18-8(키보드 입력 지원)을 범위에
추가**했고, 그 과정에서 개발용 VAN 스텁이 성공 응답에 실제 PIN을 그대로 실어 보내는 결함(H-2)을 실측으로
발견·수정했다. 상세는 `development_plan.md`의 "Phase 18 체크포인트 1", "Phase 18 최종 검증", "P18-8",
"Phase 18 실장비 검증 기록" 참고.

> **착수 전 필요했던 것(해소됨)**: 카드+자물쇠 아이콘의 배경 투명 PNG — `비밀번호 입력 아이콘.png`로 이미
> 확보돼 있었고 P18-2에서 그대로 사용했다.

---

## Phase 19 — 키오스크 시뮬레이터 (POS 역할 테스트 프로그램)

**목표**: 사람이 실제 키오스크처럼 3종 전문을 보내고 응답을 필드 단위로 확인할 수 있는 **별도 프로그램**을
만든다. 이후 Phase 20(VAN)·21(통합 검증)의 주 검증 도구가 되며, **최종적으로 키오스크 업체에 샘플 테스트
소스로 제공**된다(2026-08-26 확정) — 그래서 "우리만 쓰는 하네스"가 아니라 남이 읽을 코드로 만든다.

- [x] `src/KFTCOneCAP.KioskSim/` 신설 — **WinForms**, `net48`/**AnyCPU**, 본 앱과 별도 프로세스·별도
      프로젝트. 외부 패키지 없이 단독 빌드·실행되게 하고 주석은 한글로 쓴다(업체 제공 전제).
      **x86 고정은 하지 않는다** — 본 앱이 x86인 이유는 32bit 네이티브 DLL 2개 때문이고 시뮬레이터는
      네이티브 의존이 전혀 없다(2026-08-28 확정, `development_plan.md` Phase 19 결정 6)
- [x] **전문 필드 테이블을 본 앱의 `Protocol/Pos/`와 공유하지 않고 독립 구현한다** — 공유하면 POSITION이나
      길이를 잘못 적어도 양쪽이 똑같이 틀려 테스트가 통과해 버린다. SPEC 표를 한 번 더 옮겨 적어 서로의 오타를
      드러낸다(실제 키오스크 업체도 독립 구현하므로 더 현실적이다)
- [x] **전문 3종을 각각의 버튼**으로 배치(2026-08-26 확정) — 하나의 거래로 묶지 않고 독립 기능으로 다룬다.
      전문마다 TCP 연결을 새로 연다
- [x] 필드별 입력 UI — kiosk가 채우는 필드만 편집 가능하게 하고, 원캡/인터넷지로/VAN 담당 필드는 비활성 표시.
      미입력 필드는 자동 space 채움
- [x] 응답 전문을 **필드 단위로 분해해 표시**(raw ASCII 동시 확인) — 응답코드(`#7`)의 의미를 SPEC 3장 +
      `E`/`R`/`D` 체계로 풀어서 보여준다
- [x] 오류 주입 기능 — 길이 불일치, 알 수 없는 거래구분, 연결 중단 등을 보내 본 앱의 오류 내성을 검증

**완료 기준**: 시뮬레이터에서 3종 전문을 실제 리더기와 함께 끝까지 몰아볼 수 있고(902614는 PIN 입력 포함),
응답이 필드 단위로 확인된다. 잘못된 전문을 보내도 본 앱이 죽지 않고 `E4x` 코드로 응답한다.

**✅ Phase 19 완료(2026-08-31)** — P19-1~P19-8 전부 완료, 위 완료 기준 충족(3전문 실장비 왕복 성공,
`902614` PIN 입력 포함 — 상세 결과 `development_plan.md` "P19-8. 교차 검증 + 문서 갱신" 참고). 진행 중
발견한 본 앱 결함(`PosMessageFramer.Append`의 프레임 손실, "크리티컬"로 판정) 1건과 시뮬레이터/스키마
결함 6건도 함께 수정·재검증했다(같은 문서 "Phase 19 전체 검증(Opus)" 절 참고).

**실행계획서 작성 완료(2026-08-28)** — `development_plan.md` "Phase 19 실행계획서"(P19-1~P19-8).
착수 전 확정한 사항:

- **배포 형태**: 같은 리포 `src/KFTCOneCAP.KioskSim/` 안에 **그 폴더 전용 `.sln`을 동봉**한다.
  내부에선 루트 솔루션에도 추가하고, 업체에는 그 폴더만 압축해 주면 바로 열리고 빌드된다
  (본 앱 소스는 딸려가지 않는다).
- **오류 주입은 업체 제공본에도 포함**하되 별도 탭으로 격리한다. `#if`로 내부용/제공용을 가르지
  않는다 — 우리가 검증한 소스와 업체가 받는 소스가 완전히 같아야 재현이 가능하다.
- **입력값은 프리셋 파일(`kiosksim.preset.json`)에서 불러온다**(파일 없으면 코드 기본값).
  마지막 입력값 자동 저장은 하지 않고, 명시적 "프리셋으로 저장" 버튼만 둔다.
- **송수신 전문 로그 파일 기록은 범위 밖** — 추후 KFTCOneCAP 로그 파일 기능에서 대조한다.
- **응답 대기 타임아웃 180초 고정**(Phase 18 실측 최장 150.1초 근거), 조절 UI 없음.
- **최대 위험 구간은 P19-2(전문 필드 테이블 독립 전사)** — 여기서 SPEC을 잘못 옮기면 이후 모든
  검증이 잘못된 기준으로 통과한다. P19-2·P19-3 직후에 체크포인트 1(Opus 검증 리뷰)을 둔다.

---

## Phase 20 — VAN 연동 (`KFTC_GIRO.dll`)

**목표**: `FNAISCRDVAN` 호출로 결제 요청을 완결한다.

> **2026-08-26 확인**: POS↔원캡 구간과 **원캡↔VAN 구간의 전문 형식이 동일**하므로, Phase 17이 만든 전문을
> `inData`에 **그대로** 넣는다 — `Protocol/Van/`을 별도 설계하지 않는다. 이 점 때문에 이 Phase는 당초 계획보다
> 가벼워졌다. 출력 버퍼는 최대 전문(1504 바이트)을 기준으로 **여유를 둬 4096 바이트 고정 할당**하고 상수
> 1곳에서만 정의한다.
>
> **VAN 서버가 아직 개발 중이라 접속이 되지 않는다**(2026-08-26 사용자 확인). 따라서 이 Phase는 "호출이
> 성립하고 마샬링·버퍼가 안전한가"까지만 검증하고, 실서버 응답 검증은 미완료로 명시한다.

> `KFTC_GIRO.dll`은 **SPEC 문서가 없고** PRD §2.3의 시그니처가 유일한 계약이다. ANSI 마샬링과 버퍼 크기
> 관리를 잘못하면 **메모리 침범**이 발생한다(PRD §9) — 이 Phase에서 가장 주의할 지점.

- [ ] `Interop/KftcGiroNative.cs` — `__stdcall`, ANSI `char*` 마샬링, `outData`/`out_szRetCode` 출력 버퍼를
      **호출자가 충분히 할당**하고 크기를 명시적으로 관리(4096 바이트 상수 1곳)
- [ ] Phase 17이 만든 전문을 `inData`에 그대로 전달 — 별도 `Protocol/Van/` 전문 계층을 만들지 않는다
- [ ] Mode는 **`OT` 고정**(PRD §4.10, 2026-08-18 확정) — 운영/테스트 선택 옵션은 추후(PRD §11). 다만 나중에
      옵션화할 때 호출부를 헤집지 않도록 값을 한 곳에서만 정하게 둔다. `KFTC_GIROPOS.ini`는 미확보 상태(§10)
- [ ] 결과 판정 (PRD §4.10): `nRet == 0`이면 **DLL 통신 성공일 뿐**이므로 `outData`를 파싱해 승인/거절 판정.
      `nRet == -1`은 통신 실패. `out_szRetCode`도 함께 확인
- [ ] **"VAN DLL 통신 실패"와 "VAN 서버 거절"을 구분**해 POS에 응답, 실패 시 리더기 초기화
- [ ] 통신 중 알림창을 PROCESSING으로 전환 (PRD §4.10)
- [ ] DLL 로드/호출 실패 시에도 앱이 죽지 않을 것 (PRD §9, Phase 8의 로드 스모크와 연결)

**완료 기준**: 호출이 성립하고 마샬링·버퍼가 안전하며, 통신 실패·서버 거절·승인 3가지가 구분되어 POS까지
전달된다(각각 `D0x` / VAN 응답코드 / `000`). **VAN 서버 미개발로 실응답 검증은 불가하므로 그 범위를 명시하고
미완료로 남긴다** — INI(`KFTC_GIROPOS.ini`)도 미확보 상태다.

---

## Phase 21 — 통합 검증 & 안정성

**목표**: PRD §9를 점검하고 2차 범위를 마무리한다.

- [ ] 전체 Flow 연속 실행 — 정상/FALLBACK/취소/Timeout/VAN 거절을 섞어 반복
- [ ] 예외 내성: 리더기 케이블 분리, VAN DLL 로드 실패, 소켓 강제 종료 각각에서 **앱이 죽지 않고** 오류 처리
- [ ] 리소스 정리: 장시간 실행 후 메모리/핸들 증가 확인, 타이머·후킹·콜백 잔존 없음
- [ ] 카드 데이터가 거래 종료 후 즉시 삭제되는지 재확인 (PRD §8.4, §9)
- [ ] 계층 규칙 최종 점검: `Protocol/`만 교체하면 실제 SPEC 적용이 끝나는 구조인지 확인 (PRD §10)
- [ ] 문서 정리: PRD 미확정 사항(§10)의 현재 상태 갱신, 추후 구현 항목(§11) 정리

**완료 기준**: 위 항목이 모두 확인되고, 남은 미확정/미검증 범위가 문서에 정확히 기록된다.

---

## 남은 미확정 사항

로드맵 작성 중 발견한 항목 대부분은 2026-08-18에 확정되어 `PRD.md` §10.1 표에 정리됐다(결제 포트 선택,
baudRate, 포트 생명주기, 리더기 이중화, 무결성 2대 처리, 재요청 대상, VAN Mode, AOP 제외 등). 아직 열려
있는 것은 다음뿐이며, 해당 Phase 착수 전에 확인해 PRD를 먼저 갱신한다(작업 방식 규칙 3).

1. **알림창 크기 조절 트리거** (Phase 13) — 자산(기본/`_VERYSMALL`)만 확보된 상태. 이번 범위 밖으로 두고
   두 크기를 준비만 한다.
2. ~~**실제 통신 전문 전체**~~ — **2026-08-26 해소.** POS↔원캡 구간 전산설계서 확보로 3전문이 확정됐고,
   원캡↔VAN 구간도 **같은 전문 형식**임이 확인됐다(Phase 17·20). 남은 것은 아래 3·4번뿐이다.
3. **`#51` 비밀번호 SEED 암호화 방식** (Phase 18) — 적용 예정이나 방식 미정. 확정 전까지 평문 4자리를 그대로
   채우고, 교체 지점을 함수 1곳으로 격리해 둔다.
4. **VAN 서버 / `KFTC_GIROPOS.ini`** (Phase 20) — 서버가 아직 개발 중이라 접속 불가(2026-08-26 확인). INI도
   미확보. 실응답 검증은 서버 준비 후로 미룬다.

---

## 참고 문서
- `spec/국세 베리어프리 키오스크용 전산설계서(POS-원캡)_20260826.pdf` — **POS↔원캡 전문 SPEC 정본**(Phase 17~19).
  같은 폴더 `.hwp`는 DRM 배포용 문서 래퍼라 열리지 않는다. 필드 확인은 `pos-onecap-spec-expert` 서브에이전트에
  위임한다
- `PRD.md` — 2차 범위(결제 중계 기능) 요구사항 정의. **§10.1에 확정 사항 요약표**가 있다
- `development_plan.md` — Task 단위 실행계획서 (Phase 착수 시 작성)
- `docs/home_reader_setup/PRD_WPF.md` / `ROADMAP.md` — 1차 범위(홈·리더기 설정 화면 UX/UI) 요구사항 및 Phase 0~6 이력
- `docs/reader_dll/00_OVERVIEW.md` — Reader DLL 연동 참조 자료 인덱스
- `vendor/ReaderSerial/CSharpSample/` — C# P/Invoke 참조 구현 (`SendCommandSafe` 재시도 래퍼)
- `vendor/ReaderSerial/MfcSample/` — **리더기 이중화 페일오버 참조 구현** (`BroadcastFailover()`, 실장비 검증 완료)
- `vendor/KftcGiro/KFTC_GIRO.dll` — VAN 연동 DLL (32bit, SPEC 문서 없음 — `PRD.md` §2.3이 유일한 계약,
  Phase 8에서 `docs/payment_relay/dll/`에서 이동)
- `images/` — 결제 알림창 이미지 자산 (기본 750×650 / `_VERYSMALL` 375×325)
